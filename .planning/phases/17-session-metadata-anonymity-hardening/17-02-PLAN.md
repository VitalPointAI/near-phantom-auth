---
phase: 17
plan: 02
type: execute
wave: 1
depends_on: [17-01]
files_modified:
  - src/server/index.ts
  - src/__tests__/hooks-scaffolding.test.ts
  - src/__tests__/registration-auth.test.ts
autonomous: true
requirements: [SESSION-01, SESSION-04]
tags: [privacy, createAnonAuth, config-threading, integration-tests, backwards-compat]
must_haves:
  truths:
    - "`createAnonAuth` passes top-level `config.sessionMetadata` into `createSessionManager`."
    - "A consumer using `createAnonAuth({ sessionMetadata: { ipAddress: 'omit', userAgent: 'omit' } })` can authenticate without raw IP/user-agent reaching session persistence."
    - "Consumers omitting `sessionMetadata` keep current behavior."
  artifacts:
    - path: src/server/index.ts
      provides: "Factory threading for session metadata policy"
      contains: "metadata: config.sessionMetadata"
    - path: src/__tests__/registration-auth.test.ts
      provides: "Route-level regression coverage for omission policy"
      contains: "sessionMetadata"
---

<objective>
Thread the new session metadata policy through the public `createAnonAuth` factory and prove auth routes respect the policy through the existing session manager boundary.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Thread sessionMetadata into createSessionManager</name>
  <files>src/server/index.ts</files>
  <read_first>
    - src/server/index.ts
    - src/server/session.ts
    - src/types/index.ts
  </read_first>
  <action>
    In `src/server/index.ts`, update the `createSessionManager(db, { ... })` call so it includes:

    ```ts
    metadata: config.sessionMetadata,
    ```

    Keep `secret`, `durationMs`, and `logger` unchanged. Do not modify route handlers for this task; policy enforcement belongs in `session.ts`.
  </action>
  <verify>
    <automated>rg -n "metadata: config\\.sessionMetadata" src/server/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "metadata: config\\.sessionMetadata" src/server/index.ts` outputs `1`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add compile/threading guard for public config</name>
  <files>src/__tests__/hooks-scaffolding.test.ts</files>
  <read_first>
    - src/__tests__/hooks-scaffolding.test.ts
    - src/types/index.ts
    - src/server/index.ts
  </read_first>
  <action>
    Add a test that constructs an `AnonAuthConfig`-compatible object or calls `createAnonAuth` with:

    ```ts
    sessionMetadata: {
      ipAddress: 'omit',
      userAgent: 'omit',
    }
    ```

    The test should verify the config is accepted without TypeScript errors and factory construction does not throw when using the existing mock database/manager pattern.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/hooks-scaffolding.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "sessionMetadata" src/__tests__/hooks-scaffolding.test.ts` is at least `1`.
    - `npm test -- --run src/__tests__/hooks-scaffolding.test.ts` exits `0`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Prove anonymous auth can omit raw session metadata</name>
  <files>src/__tests__/registration-auth.test.ts</files>
  <read_first>
    - src/__tests__/registration-auth.test.ts
    - src/server/router.ts
    - src/server/session.ts
    - src/server/index.ts
  </read_first>
  <action>
    Add route-level coverage using the existing registration/login supertest harness.

    Required assertion:
    - Configure `createSessionManager` or `createAnonAuth` with `sessionMetadata: { ipAddress: 'omit', userAgent: 'omit' }`.
    - Complete a successful anonymous passkey session creation path.
    - Assert the `db.createSession` input has `ipAddress === undefined` and `userAgent === undefined`, even when the request includes a remote address and a `User-Agent` header.

    If the existing harness injects a mock `sessionManager` directly into `createRouter`, prefer a focused test that constructs the real `createSessionManager` with a mock DB and calls `createSession` with raw `ipAddress`/`userAgent`. The key route-level invariant is that callers may continue passing raw request metadata while the manager strips it centrally.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/registration-auth.test.ts src/__tests__/session.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "ipAddress.*undefined|userAgent.*undefined|sessionMetadata" src/__tests__/registration-auth.test.ts src/__tests__/session.test.ts` is at least `3`.
    - `npm test -- --run src/__tests__/registration-auth.test.ts src/__tests__/session.test.ts` exits `0`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm test -- --run src/__tests__/hooks-scaffolding.test.ts src/__tests__/registration-auth.test.ts src/__tests__/session.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Public factory accepts the policy.
- Omission policy prevents raw session metadata from reaching persistence.
- Default compatibility remains intact.
</success_criteria>

