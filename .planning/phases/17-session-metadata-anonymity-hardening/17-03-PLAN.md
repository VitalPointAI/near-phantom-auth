---
phase: 17
plan: 03
type: execute
wave: 2
depends_on: [17-01, 17-02]
files_modified:
  - src/__tests__/analytics-pii-snapshot.test.ts
  - src/__tests__/analytics-pii-leak.test.ts
  - src/__tests__/logging.test.ts
autonomous: true
requirements: [SESSION-03, SESSION-04]
tags: [privacy, pii-guards, analytics, logging, regression-tests]
must_haves:
  truths:
    - "No analytics event adds raw `ip`, `ipAddress`, `userAgent`, or session metadata fields."
    - "No session metadata normalization path logs raw IP or raw user-agent."
    - "Existing PII leak fixture remains green after adding session metadata config."
  artifacts:
    - path: src/__tests__/analytics-pii-snapshot.test.ts
      provides: "Runtime whitelist guard remains explicit about forbidden session metadata fields"
      contains: "ipAddress"
    - path: src/__tests__/logging.test.ts
      provides: "Static guard against logging session metadata source values"
      contains: "session metadata"
---

<objective>
Preserve the anonymity invariant around analytics and logging after introducing session metadata policy. This plan is a regression net: configuration may mention IP/user-agent policy, but analytics events and logs must not start carrying raw request metadata.
</objective>

<tasks>
<task type="auto">
  <name>Task 1: Extend analytics PII guards to include ipAddress</name>
  <files>src/__tests__/analytics-pii-snapshot.test.ts src/__tests__/analytics-pii-leak.test.ts</files>
  <read_first>
    - src/__tests__/analytics-pii-snapshot.test.ts
    - src/__tests__/analytics-pii-leak.test.ts
    - src/server/analytics.ts
  </read_first>
  <action>
    Extend the forbidden PII field lists to include `ipAddress` in addition to existing `ip` and `userAgent`.

    In `analytics-pii-leak.test.ts`, add one tsc-fail fixture case that attempts to add:

    ```ts
    ipAddress: '203.0.113.42'
    ```

    to a lifecycle event variant and expects `tsc --noEmit` to fail.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/analytics-pii-snapshot.test.ts src/__tests__/analytics-pii-leak.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "ipAddress" src/__tests__/analytics-pii-snapshot.test.ts src/__tests__/analytics-pii-leak.test.ts` is at least `2`.
    - `npm test -- --run src/__tests__/analytics-pii-snapshot.test.ts src/__tests__/analytics-pii-leak.test.ts` exits `0`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add static logging guard for raw session metadata</name>
  <files>src/__tests__/logging.test.ts</files>
  <read_first>
    - src/__tests__/logging.test.ts
    - src/server/session.ts
    - src/server/router.ts
    - src/server/oauth/router.ts
  </read_first>
  <action>
    Add a `describe('Session metadata logging')` test block.

    Required assertions:
    - `src/server/session.ts` log calls do not include `ipAddress`, `userAgent`, `metadata.ipAddress`, or `metadata.userAgent`.
    - `src/server/router.ts` and `src/server/oauth/router.ts` log calls do not include `req.ip` or `req.headers['user-agent']`.

    Use the same static `logCalls` extraction style already used in `logging.test.ts`.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/logging.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Session metadata logging" src/__tests__/logging.test.ts` outputs `1`.
    - `npm test -- --run src/__tests__/logging.test.ts` exits `0`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm test -- --run src/__tests__/analytics-pii-snapshot.test.ts src/__tests__/analytics-pii-leak.test.ts src/__tests__/logging.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Analytics event shape remains free of session metadata.
- Logging tests guard against raw IP/user-agent leakage.
- Existing anonymity regression suite remains green.
</success_criteria>

