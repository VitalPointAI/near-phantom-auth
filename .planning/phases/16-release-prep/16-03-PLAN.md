---
phase: 16
plan: 03
type: execute
wave: 2
depends_on: [16-01, 16-02]
files_modified:
  - .planning/phases/16-release-prep/16-SMOKE.md
autonomous: true
requirements: [RELEASE-03]
tags: [release, smoke-install, npm-pack, backwards-compat, consumer-fixture]
must_haves:
  truths:
    - "npm pack produces a local @vitalpoint/near-phantom-auth-0.7.0 tarball."
    - "A fresh temporary consumer project can install the local tarball."
    - "The fresh consumer can import `createAnonAuth` from `/server`."
    - "The fresh consumer TypeScript file can reference hooks.afterAuthSuccess, hooks.backfillKeyBundle, hooks.onAuthEvent, awaitAnalytics, and rp.relatedOrigins."
    - "The fresh consumer can import and instantiate MPCAccountManager from `/server`, preserving the v0.6.1 contract."
  artifacts:
    - path: .planning/phases/16-release-prep/16-SMOKE.md
      provides: "Smoke install transcript and backwards-compat evidence"
      contains: "0.7.0 smoke install"
---

<objective>
Prove the package that will be published works from a fresh consumer install before it reaches the npm registry.

This plan closes RELEASE-03 with a local `npm pack` smoke install and a TypeScript compile fixture that exercises the v0.7.0 hook surface plus the v0.6.1 MPCAccountManager compatibility path.
</objective>

<context>
@package.json
@dist/server/index.d.ts
@dist/server/index.js
@src/server/index.ts
@src/types/index.ts
@.planning/phases/10-mpcaccountmanager/10-06-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Create local npm tarball with npm pack</name>
  <files>package.json, dist/</files>
  <read_first>
    - package.json
    - README.md
    - CHANGELOG.md
    - dist/server/index.d.ts
  </read_first>
  <action>
    Run:

    `npm pack --json`

    Capture the tarball filename. It must end with `.tgz` and include version `0.7.0`.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.version !== '0.7.0') process.exit(1); console.log(p.name, p.version)"</automated>
  </verify>
  <acceptance_criteria>
    - `npm pack --json` exits `0`.
    - The tarball filename contains `near-phantom-auth-0.7.0`.
    - The pack output includes `dist/server/index.d.ts`, `dist/server/index.js`, and `README.md`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Smoke install tarball in a fresh temp consumer</name>
  <files>.planning/phases/16-release-prep/16-SMOKE.md</files>
  <read_first>
    - package.json
    - dist/server/index.d.ts
    - .planning/phases/10-mpcaccountmanager/10-06-SUMMARY.md
  </read_first>
  <action>
    Create a temporary directory under `/tmp`.

    In that directory, run:
    - `npm init -y`
    - `npm install /absolute/path/to/the/near-phantom-auth-0.7.0.tgz typescript @types/node`
    - create `tsconfig.json` with:

    ```json
    {
      "compilerOptions": {
        "target": "ES2022",
        "module": "NodeNext",
        "moduleResolution": "NodeNext",
        "strict": true,
        "skipLibCheck": true,
        "types": ["node"]
      }
    }
    ```

    - create `smoke.ts` containing imports from `@vitalpoint/near-phantom-auth/server`:
      - `createAnonAuth`
      - `MPCAccountManager`
      - type `AnonAuthConfig`
      - type `AfterAuthSuccessCtx`
      - type `BackfillKeyBundleCtx`
      - type `AnalyticsEvent`
      - type `RelatedOrigin`

    The fixture must build an `AnonAuthConfig` object that includes:
    - `hooks.afterAuthSuccess`
    - `hooks.backfillKeyBundle`
    - `hooks.onAuthEvent`
    - `awaitAnalytics: true`
    - `rp.relatedOrigins: [{ origin: 'https://example.co.uk', rpId: 'example.co.uk' }]`

    It must also instantiate:

    `new MPCAccountManager({ networkId: 'testnet', derivationSalt: 'smoke-salt' })`

    Run:
    - `npx tsc --noEmit`
    - `node -e "import('@vitalpoint/near-phantom-auth/server').then(m => { console.log(typeof m.createAnonAuth, typeof m.MPCAccountManager); if (typeof m.createAnonAuth !== 'function' || typeof m.MPCAccountManager !== 'function') process.exit(1); })"`

    Write `.planning/phases/16-release-prep/16-SMOKE.md` with the temp directory path, tarball filename, TypeScript result, runtime import result, and any warnings.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-release-prep/16-SMOKE.md && rg -n "0\\.7\\.0 smoke install|tsc --noEmit|function function|MPCAccountManager|backfillKeyBundle|relatedOrigins" .planning/phases/16-release-prep/16-SMOKE.md</automated>
  </verify>
  <acceptance_criteria>
    - `npx tsc --noEmit` exits `0` in the fresh temp consumer.
    - Runtime import prints `function function`.
    - `.planning/phases/16-release-prep/16-SMOKE.md` contains `0.7.0 smoke install`.
    - `.planning/phases/16-release-prep/16-SMOKE.md` contains `backfillKeyBundle`, `afterAuthSuccess`, `onAuthEvent`, `awaitAnalytics`, and `relatedOrigins`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm run typecheck`
- `npm test -- --run`
- `npm pack --json`
- fresh-consumer `npx tsc --noEmit`
</verification>

<success_criteria>
- The exact local package artifact intended for publish passes fresh-install TypeScript and runtime import checks.
- v0.6.1 direct MPCAccountManager consumer compatibility is verified against the v0.7.0 package.
</success_criteria>
