---
phase: 16
plan: 04
type: execute
wave: 3
depends_on: [16-01, 16-02, 16-03]
files_modified:
  - .planning/ROADMAP.md
  - .planning/STATE.md
  - .planning/MILESTONES.md
  - .planning/phases/16-release-prep/16-04-SUMMARY.md
autonomous: false
requirements: [RELEASE-04]
tags: [release, publish, npm, git-tag, milestone-close, manual-checkpoint]
user_setup:
  - service: npm
    why: "Publishing @vitalpoint/near-phantom-auth@0.7.0 to the public registry requires npm authentication and possibly 2FA."
    env_vars:
      - name: NPM_TOKEN
        source: "User npm account automation token, or interactive npm login"
    dashboard_config:
      - task: "Confirm npm 2FA device is available if account requires OTP."
        location: "npm account security settings"
  - service: git
    why: "Pushing tag v0.7.0 to origin requires git remote credentials."
    env_vars: []
    dashboard_config:
      - task: "Confirm origin remote is correct before pushing tag."
        location: "git remote -v"
must_haves:
  truths:
    - "npm publish for @vitalpoint/near-phantom-auth@0.7.0 succeeds."
    - "npm view @vitalpoint/near-phantom-auth@0.7.0 version returns 0.7.0."
    - "npm view @vitalpoint/near-phantom-auth dist-tags shows latest: 0.7.0."
    - "Fresh install from the npm registry, not the local tarball, passes the same runtime import and TypeScript smoke checks from Plan 16-03."
    - "git tag v0.7.0 exists and is pushed to origin."
    - "ROADMAP/STATE/MILESTONES reflect v0.7.0 release completion."
  artifacts:
    - path: .planning/phases/16-release-prep/16-04-SUMMARY.md
      provides: "Publish transcript and release closure evidence"
      contains: "@vitalpoint/near-phantom-auth@0.7.0"
---

<objective>
Publish v0.7.0 to npm, verify the registry artifact, push the git tag, and close the v0.7.0 milestone state.

This plan closes RELEASE-04. It is intentionally `autonomous: false` because npm auth/OTP and tag push are credentialed release actions.
</objective>

<context>
@package.json
@CHANGELOG.md
@README.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/MILESTONES.md
@.planning/phases/16-release-prep/16-SMOKE.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Final pre-publish gate</name>
  <files>package.json, CHANGELOG.md, README.md, dist/</files>
  <read_first>
    - package.json
    - CHANGELOG.md
    - README.md
    - .planning/phases/16-release-prep/16-SMOKE.md
  </read_first>
  <action>
    Run:
    - `node -p "require('./package.json').version"`
    - `npm run typecheck`
    - `npm test -- --run`
    - `npm run build`
    - `npm publish --dry-run --access public`

    Confirm version is `0.7.0` before continuing.
  </action>
  <verify>
    <automated>node -e "const p=require('./package.json'); if (p.version !== '0.7.0') process.exit(1); console.log(p.version)"</automated>
  </verify>
  <acceptance_criteria>
    - `node -p "require('./package.json').version"` outputs `0.7.0`.
    - `npm run typecheck` exits `0`.
    - `npm test -- --run` exits `0`.
    - `npm run build` exits `0`.
    - `npm publish --dry-run --access public` exits `0` and reports version `0.7.0`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 2: Publish @vitalpoint/near-phantom-auth@0.7.0 to npm</name>
  <files>package.json, dist/</files>
  <read_first>
    - package.json
    - .planning/phases/16-release-prep/16-SMOKE.md
  </read_first>
  <action>
    If not authenticated, run:

    `npm whoami`

    If `npm whoami` fails, authenticate with the user's npm account.

    Publish:

    `npm publish --access public`

    If npm requires OTP, rerun with:

    `npm publish --access public --otp=<6-digit-code>`

    After publish, verify:
    - `npm view @vitalpoint/near-phantom-auth@0.7.0 version`
    - `npm view @vitalpoint/near-phantom-auth dist-tags --json`
  </action>
  <verify>
    <automated>npm view @vitalpoint/near-phantom-auth@0.7.0 version</automated>
  </verify>
  <acceptance_criteria>
    - `npm publish --access public` or `npm publish --access public --otp=<code>` exits `0`.
    - `npm view @vitalpoint/near-phantom-auth@0.7.0 version` outputs `0.7.0`.
    - `npm view @vitalpoint/near-phantom-auth dist-tags --json` contains `"latest": "0.7.0"`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Smoke install from npm registry</name>
  <files>.planning/phases/16-release-prep/16-04-SUMMARY.md</files>
  <read_first>
    - .planning/phases/16-release-prep/16-SMOKE.md
    - package.json
  </read_first>
  <action>
    Create a fresh temporary directory under `/tmp`.

    Run:
    - `npm init -y`
    - `npm install @vitalpoint/near-phantom-auth@0.7.0 typescript @types/node`
    - create the same `tsconfig.json` and `smoke.ts` shape from Plan 16-03, but install from the npm registry instead of the tarball.
    - `npx tsc --noEmit`
    - `node -e "import('@vitalpoint/near-phantom-auth/server').then(m => { console.log(typeof m.createAnonAuth, typeof m.MPCAccountManager); if (typeof m.createAnonAuth !== 'function' || typeof m.MPCAccountManager !== 'function') process.exit(1); })"`

    Append the registry smoke output to `16-04-SUMMARY.md`.
  </action>
  <verify>
    <automated>test -f .planning/phases/16-release-prep/16-04-SUMMARY.md && rg -n "registry smoke|0\\.7\\.0|function function|tsc --noEmit" .planning/phases/16-release-prep/16-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - Registry smoke `npx tsc --noEmit` exits `0`.
    - Runtime import prints `function function`.
    - `16-04-SUMMARY.md` contains `registry smoke`.
  </acceptance_criteria>
</task>

<task type="checkpoint:human-action">
  <name>Task 4: Tag v0.7.0 and push to origin</name>
  <files>.git refs</files>
  <read_first>
    - package.json
    - CHANGELOG.md
  </read_first>
  <action>
    Confirm:
    - `git status --short`
    - `git remote -v`
    - `git tag --list v0.7.0`

    If no `v0.7.0` tag exists, create it:

    `git tag v0.7.0`

    Push:

    `git push origin v0.7.0`
  </action>
  <verify>
    <automated>git tag --list v0.7.0</automated>
  </verify>
  <acceptance_criteria>
    - `git tag --list v0.7.0` outputs `v0.7.0`.
    - `git push origin v0.7.0` exits `0`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 5: Close v0.7.0 planning state</name>
  <files>.planning/ROADMAP.md, .planning/STATE.md, .planning/MILESTONES.md, .planning/phases/16-release-prep/16-04-SUMMARY.md</files>
  <read_first>
    - .planning/ROADMAP.md
    - .planning/STATE.md
    - .planning/MILESTONES.md
    - .planning/phases/16-release-prep/16-SMOKE.md
  </read_first>
  <action>
    Update planning artifacts after npm publish, registry smoke, and tag push:
    - ROADMAP progress row for Phase 16 becomes `Complete` with completed date `2026-04-30`.
    - ROADMAP v0.7.0 milestone summary changes from planning/executing to shipped.
    - STATE.md `stopped_at` says `v0.7.0 published`.
    - STATE.md progress becomes `completed_phases: 6`, `completed_plans: 27`, `percent: 100`.
    - MILESTONES.md gets a `v0.7.0 Consumer Hooks & Recovery Hardening` entry with publish date, phase count, plan count, and npm package version.
    - `16-04-SUMMARY.md` records npm publish verification, registry smoke verification, and git tag push result.
  </action>
  <verify>
    <automated>rg -n "v0\\.7\\.0 published|0\\.7\\.0|Phase 16|@vitalpoint/near-phantom-auth@0\\.7\\.0" .planning/ROADMAP.md .planning/STATE.md .planning/MILESTONES.md .planning/phases/16-release-prep/16-04-SUMMARY.md</automated>
  </verify>
  <acceptance_criteria>
    - `.planning/STATE.md` contains `v0.7.0 published`.
    - `.planning/ROADMAP.md` Phase 16 progress row contains `Complete`.
    - `.planning/MILESTONES.md` contains `v0.7.0 Consumer Hooks & Recovery Hardening`.
    - `.planning/phases/16-release-prep/16-04-SUMMARY.md` contains `@vitalpoint/near-phantom-auth@0.7.0`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm view @vitalpoint/near-phantom-auth@0.7.0 version`
- registry fresh-consumer `npx tsc --noEmit`
- registry runtime import check
- `git tag --list v0.7.0`
</verification>

<success_criteria>
- RELEASE-04 is complete: package published, latest tag set, registry smoke passed, git tag pushed.
- Project planning state reflects v0.7.0 completion.
</success_criteria>
