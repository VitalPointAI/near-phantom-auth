---
phase: 16
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - README.md
  - CHANGELOG.md
autonomous: true
requirements: [RELEASE-01, RELEASE-02]
tags: [release, docs, changelog, readme, additive-only, v0.7.0]
must_haves:
  truths:
    - "README.md contains a top-level `## Hooks (v0.7.0)` section before `## Installation`."
    - "`## Hooks (v0.7.0)` mentions `hooks.afterAuthSuccess`, `hooks.backfillKeyBundle`, `hooks.onAuthEvent`, `awaitAnalytics`, and `rp.relatedOrigins`."
    - "`## Hooks (v0.7.0)` includes the consumer-owned schema callout for backfill, the MPC orphan trade-off for second factor, the anonymity invariant for analytics, and the 5-origin cap for related origins."
    - "CHANGELOG.md contains a top-level `## [0.7.0] - 2026-04-30` entry above `## [0.6.1]`."
    - "The v0.7.0 changelog entry explicitly says additive-only / no breaking changes from v0.6.1."
  artifacts:
    - path: README.md
      provides: "RELEASE-01 consolidated v0.7.0 feature documentation"
      contains: "## Hooks (v0.7.0)"
    - path: CHANGELOG.md
      provides: "RELEASE-02 v0.7.0 release notes"
      contains: "## [0.7.0]"
---

<objective>
Create the release-facing documentation for v0.7.0 before any version bump or publish work.

This plan closes RELEASE-01 and RELEASE-02: README gets a consolidated "Hooks (v0.7.0)" section that ties together the five v0.7.0 surfaces, and CHANGELOG.md gets a first-class v0.7.0 entry with an additive-only compatibility note.
</objective>

<context>
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@README.md
@CHANGELOG.md
@.planning/phases/12-multi-rp-id-verification/12-04-SUMMARY.md
@.planning/phases/13-registration-analytics-hook/13-05-SUMMARY.md
@.planning/phases/14-second-factor-enrolment-hook/14-04-SUMMARY.md
@.planning/phases/15-lazy-backfill-hook/15-04-SUMMARY.md
</context>

<tasks>
<task type="auto">
  <name>Task 1: Add consolidated README "Hooks (v0.7.0)" section</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - .planning/ROADMAP.md Phase 16 section
    - .planning/REQUIREMENTS.md RELEASE-01
    - .planning/phases/14-second-factor-enrolment-hook/14-04-SUMMARY.md
    - .planning/phases/15-lazy-backfill-hook/15-04-SUMMARY.md
  </read_first>
  <action>
    Insert a new top-level README section exactly titled `## Hooks (v0.7.0)` before `## Installation`.

    The section must include:
    - a one-paragraph additive-only overview: "v0.7.0 adds consumer extension points without removing or renaming the v0.6.1 API surface."
    - a bullet/table entry for `hooks.afterAuthSuccess` describing inline second-factor gating before session creation.
    - a bullet/table entry for `hooks.backfillKeyBundle` describing pass-through lazy key-bundle migration on `/login/finish` when `sealingKeyHex` exists.
    - a bullet/table entry for `hooks.onAuthEvent` describing privacy-preserving lifecycle analytics and `awaitAnalytics`.
    - a bullet/table entry for `rp.relatedOrigins` describing cross-domain passkeys and the 5-origin cap.
    - explicit callouts containing these exact strings:
      - `consumer-owned schema`
      - `MPC orphan trade-off`
      - `anonymity invariant`
      - `5 related origins`
      - `awaitAnalytics`
    - links or references to the existing detailed sections:
      - `Cross-Domain Passkeys (v0.7.0)`
      - `Second-Factor Enrolment Hook (v0.7.0)`
      - `Lazy-Backfill Hook (v0.7.0)`
  </action>
  <verify>
    <automated>rg -n "## Hooks \\(v0\\.7\\.0\\)|hooks\\.afterAuthSuccess|hooks\\.backfillKeyBundle|hooks\\.onAuthEvent|awaitAnalytics|rp\\.relatedOrigins|consumer-owned schema|MPC orphan trade-off|anonymity invariant|5 related origins" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "^## Hooks \\(v0\\.7\\.0\\)$" README.md` outputs `1`.
    - `rg -n "^## Hooks \\(v0\\.7\\.0\\)$|^## Installation$" README.md` shows the Hooks section line before Installation.
    - `rg -c "hooks\\.afterAuthSuccess" README.md` is at least `1`.
    - `rg -c "hooks\\.backfillKeyBundle" README.md` is at least `1`.
    - `rg -c "hooks\\.onAuthEvent" README.md` is at least `1`.
    - `rg -c "awaitAnalytics" README.md` is at least `1`.
    - `rg -c "rp\\.relatedOrigins" README.md` is at least `1`.
    - `rg -c "consumer-owned schema|MPC orphan trade-off|anonymity invariant|5 related origins" README.md` is at least `4`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add v0.7.0 CHANGELOG entry</name>
  <files>CHANGELOG.md</files>
  <read_first>
    - CHANGELOG.md
    - .planning/ROADMAP.md Phase 16 section
    - .planning/REQUIREMENTS.md RELEASE-02
    - .planning/phases/11-backup-eligibility-flags-hooks-scaffolding/11-06-SUMMARY.md
    - .planning/phases/12-multi-rp-id-verification/12-04-SUMMARY.md
    - .planning/phases/13-registration-analytics-hook/13-05-SUMMARY.md
    - .planning/phases/14-second-factor-enrolment-hook/14-04-SUMMARY.md
    - .planning/phases/15-lazy-backfill-hook/15-04-SUMMARY.md
  </read_first>
  <action>
    Add a new changelog entry above `## [0.6.1] — 2026-04-29`:

    `## [0.7.0] — 2026-04-30`

    Required subsections:
    - `### Added`
    - `### Compatibility`
    - `### Notes`

    The `### Added` subsection must mention:
    - backup-eligibility response flags
    - multi-RP_ID / related-origin verification
    - `hooks.onAuthEvent` and `awaitAnalytics`
    - `hooks.afterAuthSuccess`
    - `hooks.backfillKeyBundle`

    The `### Compatibility` subsection must include the exact phrase:
    `Additive only - no breaking changes from v0.6.1.`

    The `### Notes` subsection must mention:
    - `MPCAccountManager` contract remains frozen
    - zero new npm dependencies
    - consumer-owned schema for lazy backfill
  </action>
  <verify>
    <automated>rg -n "## \\[0\\.7\\.0\\]|Additive only - no breaking changes from v0\\.6\\.1|hooks\\.afterAuthSuccess|hooks\\.backfillKeyBundle|hooks\\.onAuthEvent|awaitAnalytics|related-origin|MPCAccountManager|zero new npm dependencies" CHANGELOG.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "^## \\[0\\.7\\.0\\] — 2026-04-30$" CHANGELOG.md` outputs `1`.
    - `rg -n "^## \\[0\\.7\\.0\\]|^## \\[0\\.6\\.1\\]" CHANGELOG.md` shows `0.7.0` before `0.6.1`.
    - `rg -c "Additive only - no breaking changes from v0\\.6\\.1\\." CHANGELOG.md` outputs `1`.
    - `rg -c "hooks\\.afterAuthSuccess|hooks\\.backfillKeyBundle|hooks\\.onAuthEvent|awaitAnalytics|related-origin|backup-eligibility" CHANGELOG.md` is at least `6`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm run typecheck`
- `npm test -- --run src/__tests__/exports.test.ts src/__tests__/hooks-scaffolding.test.ts`
</verification>

<success_criteria>
- RELEASE-01 README coverage is present and grep-verifiable.
- RELEASE-02 changelog entry is present and additive-only compatibility is explicit.
- No package version or dist changes are introduced in this plan.
</success_criteria>
