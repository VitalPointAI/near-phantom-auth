---
phase: 18
plan: 06
type: execute
wave: 3
depends_on: [18-05]
files_modified:
  - src/server/enterprise/scim.ts
  - src/__tests__/scim-groups.test.ts
  - src/__tests__/enterprise-anonymity.test.ts
autonomous: true
requirements: [ENT-06]
tags: [enterprise, scim, groups, service-provider-config, policy-boundary]
must_haves:
  truths:
    - "SCIM Groups endpoints exist for lifecycle compatibility."
    - "Group membership is stored only as enterprise external attributes."
    - "No package code maps groups to roles, permissions, scopes, or product policy."
    - "ServiceProviderConfig advertises the supported subset."
  artifacts:
    - path: src/server/enterprise/scim.ts
      provides: "SCIM Groups and ServiceProviderConfig endpoints"
      contains: "ServiceProviderConfig"
---

<objective>
Complete the SCIM subset with Groups and ServiceProviderConfig while preserving the package/application policy boundary.
</objective>

<threat_model>
T-18-02 cross-track and policy leakage: SCIM group names can be PII or policy-bearing. The package must store them only as enterprise attributes and must not interpret them as roles or permissions.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add ServiceProviderConfig endpoint</name>
  <files>src/server/enterprise/scim.ts</files>
  <read_first>
    - src/server/enterprise/scim.ts
    - specs/near-phantom-auth-enterprise-spec.md
  </read_first>
  <action>
    Add `GET /ServiceProviderConfig` returning a SCIM 2.0 response with:
    - `schemas: ['urn:ietf:params:scim:schemas:core:2.0:ServiceProviderConfig']`
    - `patch.supported: true`
    - `bulk.supported: false`
    - `filter.supported: true`
    - `changePassword.supported: false`
    - `sort.supported: false`
    - `etag.supported: false`
    - `authenticationSchemes` describing bearer token auth without including the configured token
  </action>
  <verify>
    <automated>rg -n "ServiceProviderConfig|authenticationSchemes|changePassword|bulk" src/server/enterprise/scim.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "ServiceProviderConfig" src/server/enterprise/scim.ts` is at least `2`.
    - `rg -n "bearerToken" src/server/enterprise/scim.ts` does not appear inside a JSON response object.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add SCIM Groups endpoints as attribute-only lifecycle</name>
  <files>src/server/enterprise/scim.ts</files>
  <read_first>
    - src/server/enterprise/scim.ts
    - src/types/index.ts
  </read_first>
  <action>
    Implement the subset:
    - `GET /Groups`
    - `POST /Groups`
    - `GET /Groups/:id`
    - `PATCH /Groups/:id`

    Keep group state in memory only if no durable group table is planned, or store group refs in enterprise user `externalAttrs.groups` when users are patched. Do not add role mapping, permission mapping, scope mapping, dashboard mapping, or audit sink behavior.

    For group membership updates, write group display names or ids into `enterprise_users.external_attrs.groups` through `updateEnterpriseExternalAttrs`. Responses must be SCIM-shaped Group resources with `schemas`, `id`, `displayName`, and `members`.

    Unsupported group operations return SCIM 400 errors.
  </action>
  <verify>
    <automated>rg -n "router\\.(get|post|patch)\\('/Groups|externalAttrs|groups|displayName" src/server/enterprise/scim.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "router.post\\('/Groups'" src/server/enterprise/scim.ts` outputs `1`.
    - `rg -c "router.patch\\('/Groups/:id'" src/server/enterprise/scim.ts` outputs `1`.
    - `rg -n "role|permission|scope" src/server/enterprise/scim.ts` returns no policy-mapping implementation.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Test groups and policy boundary</name>
  <files>src/__tests__/scim-groups.test.ts, src/__tests__/enterprise-anonymity.test.ts</files>
  <read_first>
    - src/__tests__/scim-users.test.ts
    - src/__tests__/enterprise-anonymity.test.ts
    - src/server/enterprise/scim.ts
  </read_first>
  <action>
    Add tests that:
    - `GET /ServiceProviderConfig` returns supported feature flags and no token.
    - missing/wrong bearer protects Groups and ServiceProviderConfig routes the same way as Users.
    - `POST /Groups` returns a SCIM Group response.
    - `PATCH /Groups/:id` updates group membership without invoking any role/permission mapper.
    - source grep asserts `src/server/enterprise/scim.ts` contains no `roleToPermission`, `permissionMap`, `scopeMap`, or `auditLog`.
    - anonymous routes still do not return group data.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/scim-groups.test.ts src/__tests__/enterprise-anonymity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/__tests__/scim-groups.test.ts src/__tests__/enterprise-anonymity.test.ts` exits `0`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm test -- --run src/__tests__/scim-groups.test.ts src/__tests__/scim-users.test.ts src/__tests__/enterprise-anonymity.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- SCIM Groups and ServiceProviderConfig endpoints are implemented.
- Group handling stays mechanism-only.
- No product role/scope/audit policy enters the package.
</success_criteria>

