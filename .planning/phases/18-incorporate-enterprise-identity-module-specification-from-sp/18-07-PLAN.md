---
phase: 18
plan: 07
type: execute
wave: 4
depends_on: [18-01, 18-02, 18-03, 18-04, 18-05, 18-06]
files_modified:
  - README.md
  - CHANGELOG.md
  - src/__tests__/enterprise-docs.test.ts
  - src/__tests__/enterprise-anonymity.test.ts
autonomous: true
requirements: [ENT-07, ENT-08]
tags: [enterprise, docs, anonymity-audit, prf, dek, release-hardening]
must_haves:
  truths:
    - "README documents enterprise as opt-in and off by default."
    - "README anonymity audit describes three tracks and says anonymous core remains default."
    - "PRF/DEK interplay is documented for passkey step-up and server-managed DEK."
    - "CHANGELOG describes the enterprise module as additive."
  artifacts:
    - path: README.md
      provides: "Enterprise module consumer documentation"
      contains: "Enterprise Identity Binding"
---

<objective>
Document the enterprise module, update the privacy/anonymity audit, and run final hardening checks for Phase 18.
</objective>

<threat_model>
T-18-01 default anonymity regression: docs must not imply enterprise is part of the default anonymous install.

T-18-02 cross-track PII leakage: docs must clearly tell consumers enterprise PII lives only on the enterprise track and must not be copied into anonymous users or analytics events.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add README enterprise setup section</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - specs/near-phantom-auth-enterprise-spec.md
    - src/types/index.ts
    - src/server/index.ts
  </read_first>
  <action>
    Add a top-level README section named `Enterprise Identity Binding` or `Enterprise Identity Module`.

    The section must include:
    - explicit statement: absent `enterprise` config means today's anonymous-first behavior remains unchanged
    - `createAnonAuth({ enterprise: { enabled: true, ... } })` example
    - `auth.enterprise.linkIdentity` / `unlinkIdentity` / `resolveByExternalId` examples
    - `auth.scimRouter` mount example under `/scim/v2`
    - SCIM bearer token responsibility
    - statement that OIDC/SAML connectors are future/promote-later work
    - statement that role-to-permission mapping and audit log sink are application policy
  </action>
  <verify>
    <automated>rg -n "Enterprise Identity|scimRouter|linkIdentity|role|OIDC|SAML" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "Enterprise Identity" README.md` is at least `1`.
    - `rg -c "scimRouter" README.md` is at least `1`.
    - `rg -c "off by default|absent.*enterprise" README.md` is at least `1`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Update anonymity audit and PRF/DEK documentation</name>
  <files>README.md</files>
  <read_first>
    - README.md
    - specs/near-phantom-auth-enterprise-spec.md
    - src/server/analytics.ts
  </read_first>
  <action>
    Update the Privacy and Anonymity Audit section to describe:
    - anonymous track: `anon_users`, no enterprise PII
    - OAuth track: `oauth_users`, existing PII track
    - enterprise track: `enterprise_users`, opt-in external IdP attributes
    - no joins from enterprise into `anon_users`
    - no enterprise identifiers in `AnalyticsEvent`

    Add a PRF/DEK subsection:
    - `enterprise.passkeyStepUp: true` lets consumers require passkey registration/use after IdP auth to obtain authenticator-rooted PRF sealing keys
    - `enterprise.serverManagedDek: true` documents server/enclave-managed DEK for IdP-only enterprise users
    - pure IdP enterprise auth without step-up does not have authenticator-rooted DEK properties
  </action>
  <verify>
    <automated>rg -n "enterprise_users|passkeyStepUp|serverManagedDek|AnalyticsEvent|anon_users" README.md</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "enterprise_users" README.md` is at least `1`.
    - `rg -c "passkeyStepUp" README.md` is at least `1`.
    - `rg -c "serverManagedDek" README.md` is at least `1`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add changelog and docs regression tests</name>
  <files>CHANGELOG.md, src/__tests__/enterprise-docs.test.ts, src/__tests__/enterprise-anonymity.test.ts</files>
  <read_first>
    - CHANGELOG.md
    - src/__tests__/enterprise-anonymity.test.ts
    - README.md
  </read_first>
  <action>
    Add a CHANGELOG entry for the enterprise module. It must say additive, opt-in, off by default, and include highlights for binding API, SCIM, session offboarding, and docs.

    Create/extend `src/__tests__/enterprise-docs.test.ts` with grep assertions for:
    - README has enterprise opt-in/off-by-default language
    - README documents `auth.scimRouter`
    - README documents `passkeyStepUp`
    - README documents `serverManagedDek`
    - CHANGELOG mentions enterprise additive/opt-in behavior

    Extend `enterprise-anonymity.test.ts` to assert README states enterprise PII is separate from anonymous users.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/enterprise-docs.test.ts src/__tests__/enterprise-anonymity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `npm test -- --run src/__tests__/enterprise-docs.test.ts src/__tests__/enterprise-anonymity.test.ts` exits `0`.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 4: Final Phase 18 verification</name>
  <files>README.md, CHANGELOG.md, src/__tests__/enterprise-docs.test.ts</files>
  <read_first>
    - package.json
    - vitest.config.ts
    - tsconfig.json
    - .planning/phases/18-incorporate-enterprise-identity-module-specification-from-sp/18-VALIDATION.md
  </read_first>
  <action>
    Run the full Phase 18 focused suite, then typecheck and build:
    - `npm test -- --run src/__tests__/enterprise-config.test.ts src/__tests__/enterprise-binding.test.ts src/__tests__/enterprise-session.test.ts src/__tests__/scim-users.test.ts src/__tests__/scim-groups.test.ts src/__tests__/enterprise-anonymity.test.ts src/__tests__/enterprise-docs.test.ts`
    - `npm run typecheck`
    - `npm run build`

    If any test fails because of missing implementation from earlier plans, fix the earlier code rather than loosening tests.
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/enterprise-config.test.ts src/__tests__/enterprise-binding.test.ts src/__tests__/enterprise-session.test.ts src/__tests__/scim-users.test.ts src/__tests__/scim-groups.test.ts src/__tests__/enterprise-anonymity.test.ts src/__tests__/enterprise-docs.test.ts</automated>
    <automated>npm run typecheck</automated>
    <automated>npm run build</automated>
  </verify>
  <acceptance_criteria>
    - Focused Phase 18 suite exits `0`.
    - `npm run typecheck` exits `0`.
    - `npm run build` exits `0`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm test -- --run src/__tests__/enterprise-config.test.ts src/__tests__/enterprise-binding.test.ts src/__tests__/enterprise-session.test.ts src/__tests__/scim-users.test.ts src/__tests__/scim-groups.test.ts src/__tests__/enterprise-anonymity.test.ts src/__tests__/enterprise-docs.test.ts`
- `npm run typecheck`
- `npm run build`
</verification>

<success_criteria>
- Enterprise docs are complete and explicit about opt-in behavior.
- Anonymity audit covers the three-track model.
- PRF/DEK tradeoffs are documented.
- Final focused suite, typecheck, and build are green.
</success_criteria>

