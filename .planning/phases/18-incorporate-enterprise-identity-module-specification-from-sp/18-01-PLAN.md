---
phase: 18
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/types/index.ts
  - src/server/index.ts
  - src/__tests__/enterprise-config.test.ts
  - src/__tests__/enterprise-anonymity.test.ts
autonomous: true
requirements: [ENT-01, ENT-07]
tags: [enterprise, config, default-off, types, exports, wave-0]
must_haves:
  truths:
    - "Enterprise config is fully optional and disabled by default."
    - "createAnonAuth without enterprise config exposes no scimRouter and does not initialize enterprise schema."
    - "Enterprise public types are additive and exported from /server."
    - "PRF/DEK interplay config keys exist without changing anonymous/passkey defaults."
  artifacts:
    - path: src/types/index.ts
      provides: "Enterprise config, user, event, adapter, and session-track type contracts"
      contains: "EnterpriseConfig"
    - path: src/server/index.ts
      provides: "Optional enterprise/scim fields on AnonAuthInstance with default-off behavior"
      contains: "scimRouter?"
---

<objective>
Create the enterprise module type/config foundation and Wave 0 default-off tests. This plan must not implement SCIM or binding behavior yet; it locks the public contract and proves consumers without `enterprise` config keep the existing behavior.
</objective>

<threat_model>
T-18-01 default anonymity regression: a consumer who omits `enterprise` must not see new routes, schema initialization, session behavior, or required adapter methods. Mitigate with optional config, optional instance fields, and regression tests.

T-18-02 cross-track PII leakage: enterprise fields must not be added to `AnonUser`, anonymous responses, or `AnalyticsEvent`. Mitigate with separate `EnterpriseUser` types and grep/type assertions.
</threat_model>

<tasks>
<task type="auto">
  <name>Task 1: Add enterprise public type contract</name>
  <files>src/types/index.ts</files>
  <read_first>
    - src/types/index.ts
    - specs/near-phantom-auth-enterprise-spec.md
    - .planning/phases/18-incorporate-enterprise-identity-module-specification-from-sp/18-RESEARCH.md
  </read_first>
  <action>
    Add additive enterprise types in `src/types/index.ts`:
    - `EnterpriseStatus = 'active' | 'suspended' | 'deprovisioned'`
    - `SessionTrack = 'anonymous' | 'oauth' | 'enterprise'`
    - `EnterpriseUser` with `id`, `type: 'enterprise'`, `nearAccountId`, `externalIdp`, `externalSub`, `externalAttrs?`, `scimId?`, `status`, `createdAt`, `updatedAt`
    - `CreateEnterpriseUserInput`
    - `EnterpriseConfig` with optional `enabled`, `binding.mintMpcIfMissing`, `passkeyStepUp`, `serverManagedDek`, and nested `scim.enabled`, `scim.bearerToken`, `scim.attributeMapping`
    - `EnterpriseEventMap` for `identity.linked`, `identity.unlinked`, `identity.status`, `scim.provisioned`, `scim.deprovisioned`
    - `EnterpriseBindingApi` method signatures from the spec

    Add `enterprise?: EnterpriseConfig` to `AnonAuthConfig`. Extend `DatabaseAdapter` with optional enterprise methods named `initializeEnterprise`, `createEnterpriseUser`, `getEnterpriseUserById`, `getEnterpriseUserByExternalId`, `getEnterpriseUserByNearAccount`, `getEnterpriseUserByScimId`, `updateEnterpriseUser`, `setEnterpriseUserStatus`, `deleteEnterpriseUser`, and `updateEnterpriseExternalAttrs`.

    Do not add enterprise fields to `AnonUser`, `OAuthUser`, or `AnalyticsEvent`.
  </action>
  <verify>
    <automated>rg -n "EnterpriseConfig|EnterpriseUser|EnterpriseBindingApi|SessionTrack|initializeEnterprise" src/types/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "export interface EnterpriseConfig" src/types/index.ts` outputs `1`.
    - `rg -c "enterprise\\?: EnterpriseConfig" src/types/index.ts` outputs `1`.
    - `rg -c "type: 'enterprise'" src/types/index.ts` is at least `1`.
    - `rg -n "AnalyticsEvent" src/server/analytics.ts` still shows no enterprise event variants.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 2: Add default-off instance shape and exports</name>
  <files>src/server/index.ts</files>
  <read_first>
    - src/server/index.ts
    - src/types/index.ts
    - src/server/oauth/router.ts
  </read_first>
  <action>
    Extend `AnonAuthInstance` with optional `enterprise?: EnterpriseBindingApi` and `scimRouter?: Router`. Re-export all new enterprise public types from `/server`.

    In `createAnonAuth`, compute `enterpriseEnabled = config.enterprise?.enabled === true`. For this plan, leave `enterprise` and `scimRouter` undefined even when enabled until later plans wire implementations. Preserve the existing return shape for all current fields.

    Update `initialize()` so it calls `await db.initialize()` exactly as before. Do not call any enterprise adapter method in this plan.
  </action>
  <verify>
    <automated>rg -n "enterprise\\?: EnterpriseBindingApi|scimRouter\\?: Router|EnterpriseConfig|EnterpriseUser" src/server/index.ts</automated>
  </verify>
  <acceptance_criteria>
    - `rg -c "scimRouter\\?: Router" src/server/index.ts` outputs `1`.
    - `rg -c "enterprise\\?: EnterpriseBindingApi" src/server/index.ts` outputs `1`.
    - `rg -n "initializeEnterprise" src/server/index.ts` returns no call site in Plan 01.
  </acceptance_criteria>
</task>

<task type="auto">
  <name>Task 3: Add Wave 0 default-off and anonymity regression tests</name>
  <files>src/__tests__/enterprise-config.test.ts, src/__tests__/enterprise-anonymity.test.ts</files>
  <read_first>
    - src/__tests__/hooks-scaffolding.test.ts
    - src/__tests__/analytics-pii-snapshot.test.ts
    - src/types/index.ts
    - src/server/index.ts
    - src/server/db/adapters/postgres.ts
  </read_first>
  <action>
    Create `src/__tests__/enterprise-config.test.ts` with tests that:
    - construct `createAnonAuth` without `enterprise`
    - assert `auth.enterprise` is `undefined`
    - assert `auth.scimRouter` is `undefined`
    - call `auth.initialize()` against a minimal custom adapter and assert `initializeEnterprise` was not called
    - assert `POSTGRES_SCHEMA` does not contain `enterprise_users`

    Create `src/__tests__/enterprise-anonymity.test.ts` with tests that:
    - grep `src/server/analytics.ts` and assert no event type starts with `enterprise.` or `scim.`
    - assert `AnonUser` source block does not contain `externalSub`, `externalAttrs`, `scimId`, or `externalIdp`
    - assert no anonymous route response in `src/server/router.ts` includes `externalAttrs`
  </action>
  <verify>
    <automated>npm test -- --run src/__tests__/enterprise-config.test.ts src/__tests__/enterprise-anonymity.test.ts</automated>
  </verify>
  <acceptance_criteria>
    - `test -f src/__tests__/enterprise-config.test.ts` exits `0`.
    - `test -f src/__tests__/enterprise-anonymity.test.ts` exits `0`.
    - Focused enterprise config/anonymity tests exit `0`.
  </acceptance_criteria>
</task>
</tasks>

<verification>
Run:
- `npm test -- --run src/__tests__/enterprise-config.test.ts src/__tests__/enterprise-anonymity.test.ts`
- `npm run typecheck`
</verification>

<success_criteria>
- Enterprise config and types compile.
- Default `createAnonAuth` behavior remains free of enterprise routes/services/schema calls.
- Wave 0 guards exist for default-off and cross-track PII invariants.
</success_criteria>

