---
phase: 15
plan: 02
type: execute
wave: 1
depends_on: [15-01]
files_modified:
  - src/server/router.ts
autonomous: true
requirements: [BACKFILL-01, BACKFILL-02, BACKFILL-03]
tags: [hooks, lazy-backfill, login-finish, fire-points, sealingKeyHex, containment, redaction, pass-through]

must_haves:
  truths:
    - "`hooks.backfillKeyBundle` is called inside `POST /login/finish` AFTER `db.getUserById` resolves successfully and BEFORE `sessionManager.createSession`, ONLY when `body.sealingKeyHex` is a defined string (BACKFILL-01)"
    - "When `body.sealingKeyHex` is undefined, the hook is NOT invoked AND no `backfill` field appears on the response (silent skip — BACKFILL-01)"
    - "When `body.sealingKeyHex` is defined but `config.hooks?.backfillKeyBundle` is undefined, the hook is NOT invoked AND no `backfill` field appears on the response (consumer didn't opt in — silent absence)"
    - "Hook returning `{ backfilled, reason? }` is echoed on the response under additive `backfill: { backfilled, reason }` key (BACKFILL-02); existing fields (`success`, `codename`, `passkey?`) and Phase 14 `secondFactor?` echo are PRESERVED unchanged"
    - "Hook throw OR rejected Promise is CAUGHT by the library (BACKFILL-03 containment) — login flow continues, the session is created via `SessionManager.createSession` (verified by mock invocation in tests; real `Set-Cookie` emission is the SessionManager implementation's responsibility, not the hook-firing code under test), response status is 200 OK with `backfill: { backfilled: false, reason: 'skipped' }`"
    - "On hook throw, library logs WARN with `redactErrorMessage(err)` payload (Error.name + first 2 stack frames; never raw `err.message`) — same redaction helper used by Phase 13 wrapAnalytics (T-15-03)"
    - "Library does NOT log `ctx.userId`, `ctx.codename`, `ctx.nearAccountId`, `ctx.req`, or `ctx.sealingKeyHex` at the fire site (T-15-01 anonymity invariant)"
    - "Hook is awaited (no fire-and-forget) — login response includes `backfill` echo, which requires the result before `res.json` (T-15-08)"
    - "`config.hooks?.backfillKeyBundle` optional-chain guard handles all of `hooks: undefined`, `hooks: {}`, and `hooks: { backfillKeyBundle: undefined }` without throwing"
    - "Phase 14 HOOK-03 fire (afterAuthSuccess) and Phase 15 BACKFILL-01 fire co-exist on /login/finish: BOTH fire when configured AND sealingKeyHex supplied; ordering is afterAuthSuccess FIRST → if continue:true, then backfillKeyBundle → emit → createSession → res.json with both echoes"
    - "When Phase 14 afterAuthSuccess returns `continue: false`, the response short-circuits before backfillKeyBundle is even invoked — no `backfill` field on the short-circuit response (sequential-fire ordering)"
    - "`nvm use 20 && npm run typecheck` green; `nvm use 20 && npm test -- --run src/__tests__/registration-auth.test.ts src/__tests__/analytics-lifecycle.test.ts src/__tests__/second-factor-login.test.ts` green (no regression in Phase 11/13/14 login test surface)"
  artifacts:
    - path: src/server/router.ts
      provides: "BACKFILL-01 fire point inside /login/finish (gated on body.sealingKeyHex); BACKFILL-02 echo on response; BACKFILL-03 try/catch + redacted WARN log + skipped fallback"
      contains: "backfillKeyBundle"
  key_links:
    - from: src/server/router.ts /login/finish handler
      to: config.hooks?.backfillKeyBundle
      via: optional-chain call gated on body.sealingKeyHex
      pattern: "config.hooks\\?.backfillKeyBundle"
    - from: hook fire site
      to: response body backfill echo
      via: object literal in res.json
      pattern: "backfill: \\{ backfilled"
    - from: hook throw catch block
      to: redactErrorMessage helper from analytics.ts
      via: import + call
      pattern: "redactErrorMessage"
---

<objective>
Wire `hooks.backfillKeyBundle` into the passkey router at the single fire point that lives in `src/server/router.ts`: `POST /login/finish` (BACKFILL-01). The hook fires AFTER `db.getUserById` resolves AND AFTER any Phase 14 `afterAuthSuccess` that returned `continue: true`, BEFORE `sessionManager.createSession` — and ONLY when `body.sealingKeyHex` is a defined string. The hook is contained: a throw or rejected Promise is caught, logged WARN with a redacted payload (using the existing `redactErrorMessage` helper from Phase 13 analytics module), and the login response continues normally with `backfill: { backfilled: false, reason: 'skipped' }`. **Backfill failure NEVER blocks login** (BACKFILL-03 — the load-bearing contract).

Purpose: Phase 15's only call-site plan. Mirrors Phase 14 HOOK-03 wiring pattern (same handler, same fire window) but with a different containment model — Phase 14 `afterAuthSuccess` throw triggers a 500 (login fails); Phase 15 `backfillKeyBundle` throw is contained (login succeeds anyway). The two hooks co-exist: `afterAuthSuccess` gates the rest of the flow on `continue:false`, while `backfillKeyBundle` is purely additive observability that fires only on the `continue:true` branch.

Output: 1 source file modified. Wave 0 stubs from Plan 15-01 remain `it.todo`; Plan 15-03 turns them green.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/REQUIREMENTS.md
@.planning/STATE.md
@.planning/phases/15-lazy-backfill-hook/15-01-SUMMARY.md
@.planning/phases/14-second-factor-enrolment-hook/14-02-SUMMARY.md
@.planning/phases/13-registration-analytics-hook/13-02-SUMMARY.md

<interfaces>
The /login/finish handler post-Phase 14 lives at src/server/router.ts:371-463. The Phase 15 BACKFILL-01 INSERT POINT is between the existing `if (secondFactor) { return res.status(...).json(...); }` block and the `await sessionManager.createSession(...)` call. The Phase 14 HOOK-03 fire (around line 405) and the existing `login.finish.success` emit (around line 421) are UNCHANGED.

Key call-site prerequisites (verified 2026-04-30):
- `src/server/validation/schemas.ts:89` — `sealingKeyHex: z.string().regex(/^[0-9a-f]{64}$/).optional()`. After Zod parse, `body.sealingKeyHex` has type `string | undefined`.
- `src/server/analytics.ts:109-119` exports a named function `redactErrorMessage(err: unknown): { name: string; stackHead?: string }` that filters V8 stack frames so `err.message` never reaches the log payload. Plan 15-02 imports this helper alongside the existing `wrapAnalytics` import (currently `import { wrapAnalytics } from './analytics.js';` at router.ts:22).
- `src/types/index.ts` post-Plan 15-01 exports `BackfillKeyBundleCtx`, `BackfillKeyBundleResult`, `BackfillReason`, and tightens `AnonAuthHooks.backfillKeyBundle` to `(ctx: BackfillKeyBundleCtx) => Promise<BackfillKeyBundleResult>`.
- `src/types/index.ts` `AuthenticationFinishResponse` post-Plan 15-01 has the new `backfill?: { backfilled: boolean; reason?: BackfillReason }` field appended after `secondFactor?`.
- The current /login/finish handler destructures `const { challengeId, response } = body;` at line ~376. Plan 15-02 widens this to `const { challengeId, response, sealingKeyHex } = body;` to bind the optional sealingKeyHex into scope.
- The existing `res.json({ success: true, codename: user.codename, ...(passkeyData && { passkey: { ... } }) });` block at lines ~448-457 is the response shape Plan 15-02 augments by appending `...(backfill && { backfill })` at the end of the object literal (mirroring the existing `passkey?` spread-guard idiom).

Cross-plan ordering (load-bearing): the Phase 14 HOOK-03 fire site at line ~405 already implements the `if (config.hooks?.afterAuthSuccess) { ... if (result.continue === false) { secondFactor = ... } }` pattern. The Phase 14 `if (secondFactor) { return res.status(...).json(...); }` short-circuit branch RETURNS — so the BACKFILL fire that Plan 15-02 inserts AFTER that branch is unreachable on `continue:false`. This is the locked sequential-fire ordering (afterAuthSuccess gates backfill).
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire BACKFILL-01..03 fire point inside POST /login/finish</name>
  <files>src/server/router.ts</files>
  <read_first>
    - .planning/phases/15-lazy-backfill-hook/15-01-SUMMARY.md (Plan 15-01 outcome — types tightened, backfill? added to AuthenticationFinishResponse)
    - .planning/phases/14-second-factor-enrolment-hook/14-02-PLAN.md (precedent for the /login/finish hook fire pattern — Phase 14 HOOK-03)
    - .planning/phases/14-second-factor-enrolment-hook/14-02-SUMMARY.md (precedent — what Phase 14 actually committed to /login/finish; verify line ordering)
    - .planning/phases/13-registration-analytics-hook/13-02-SUMMARY.md (precedent for `redactErrorMessage` redaction discipline — same helper)
    - src/server/router.ts (read entirely — touches imports at line 22, /login/finish handler at lines 371-463)
    - src/server/analytics.ts (read line 109-119 — verify `redactErrorMessage` is exported as a named function so router.ts can import it)
    - src/server/validation/schemas.ts (verify line 89 — sealingKeyHex regex validation; the `body.sealingKeyHex` field is `string | undefined` after parse)
    - src/types/index.ts:126-200 (post-Plan 15-01 — verify BackfillKeyBundleCtx + BackfillKeyBundleResult are exported, AnonAuthHooks.backfillKeyBundle is tightened)
    - src/__tests__/second-factor-login.test.ts (precedent harness — verifies the existing Phase 14 fire site keeps working after Plan 15-02 edits)
  </read_first>
  <behavior>
    - The hook MUST fire on /login/finish ONLY when `body.sealingKeyHex` is a defined string AND `config.hooks?.backfillKeyBundle` is configured (silent skip when either is absent — BACKFILL-01).
    - The hook MUST be invoked AFTER the Phase 14 secondFactor short-circuit branch (so Phase 14 `continue:false` skips backfill) AND BEFORE `sessionManager.createSession`.
    - The hook result MUST be echoed on the response under `backfill: { backfilled, reason }` (additive — preserves existing `success`, `codename`, `passkey?` fields — BACKFILL-02).
    - Hook throw OR rejected Promise MUST be caught: `sessionManager.createSession` IS still called, response status IS 200 OK, `backfill: { backfilled: false, reason: 'skipped' }` is echoed, and a single WARN log entry is emitted with `redactErrorMessage(err)` payload (BACKFILL-03 + T-15-03).
    - Library MUST NOT log the ctx fields anywhere — no `log.*({ ctx, ... })` call (T-15-01).
  </behavior>
  <action>
    Edit `src/server/router.ts` /login/finish handler (currently lines 371-463). One import change + three surgical edits to the handler.

    **Step A — Augment the analytics import** at line ~22.

    Find the current line: `import { wrapAnalytics } from './analytics.js';`

    Replace with: `import { wrapAnalytics, redactErrorMessage } from './analytics.js';`

    **Step B — Destructure `sealingKeyHex` from the validated body** in /login/finish.

    Find (currently line ~376): `        const { challengeId, response } = body;`

    Replace with: `        const { challengeId, response, sealingKeyHex } = body;`

    `body.sealingKeyHex` is typed `string | undefined` after Zod parse (the schema marks it `.optional()` per `src/server/validation/schemas.ts:89`). The destructure binds it to a local of that type.

    **Step C — Insert the BACKFILL fire AFTER the Phase 14 secondFactor short-circuit and BEFORE `sessionManager.createSession`.**

    Locate the existing block (currently lines ~430-446):

    ```ts
          if (secondFactor) {
            // HOOK-05 short-circuit response. NO Set-Cookie because sessionManager.
            // createSession was never called (T-14-02 cookie leak mitigation).
            return res.status(secondFactor.status).json({
              ...secondFactor.body,
              secondFactor,
            });
          }

          // continue:true — proceed with normal session creation + standard response.
          // Per D-LOGIN-NEARACCOUNTID: keep existing { success, codename } shape — do NOT add nearAccountId.
          // Per Pattern S4: append the new passkey key at end with spread guard so a degraded
          // path with no passkeyData still returns a valid { success, codename } response.
          await sessionManager.createSession(user.id, res, {
            ipAddress: req.ip,
            userAgent: req.headers['user-agent'],
          });
    ```

    Insert BETWEEN the closing brace of the `if (secondFactor)` block and the `await sessionManager.createSession(...)` call. The new block (paste verbatim):

    ```ts
          // Phase 15 BACKFILL-01..03 fire point.
          // Pass-through hook for lazy key-bundle backfill. Fires ONLY when
          // sealingKeyHex was supplied in the request body (no PRF -> no fresh
          // sealing key -> silent skip; the hook is NOT invoked, no `backfill`
          // field appears on the response).
          //
          // BACKFILL-03 CONTAINMENT (load-bearing): a hook throw or rejected
          // Promise is caught here. The library logs WARN with redactErrorMessage(err)
          // (Error.name + first 2 stack frames; sealingKeyHex NEVER appears in
          // the log payload — same redaction helper as Phase 13 wrapAnalytics, T-15-03)
          // and continues the login flow. createSession IS still called below;
          // response carries `backfill: { backfilled: false, reason: 'skipped' }`.
          // Backfill failure MUST NEVER block login.
          //
          // T-15-01: NO `log.info({ ctx })` call here — ctx fields (userId,
          // codename, nearAccountId, sealingKeyHex, req) are NEVER written to
          // the library log payload.
          let backfill: { backfilled: boolean; reason?: 'already-current' | 'no-legacy-data' | 'completed' | 'skipped' } | undefined;
          if (sealingKeyHex && config.hooks?.backfillKeyBundle) {
            try {
              const result = await config.hooks.backfillKeyBundle({
                userId: user.id,
                codename: user.codename,
                nearAccountId: user.nearAccountId,
                sealingKeyHex,
                req,
              });
              backfill = { backfilled: result.backfilled, reason: result.reason };
            } catch (err) {
              // BACKFILL-03 containment — log redacted, fall through to skipped.
              log.warn({ err: redactErrorMessage(err) }, 'backfill hook threw');
              backfill = { backfilled: false, reason: 'skipped' };
            }
          }
    ```

    **Step D — Append the `backfill` echo to the response** (currently the existing `res.json({...})` at lines ~448-457).

    Find:

    ```ts
          res.json({
            success: true,
            codename: user.codename,
            ...(passkeyData && {
              passkey: {
                backedUp: passkeyData.backedUp,
                backupEligible: deriveBackupEligibility(passkeyData.deviceType),
              },
            }),
          });
    ```

    Replace with (additive — preserves all existing fields, appends new spread guard at end):

    ```ts
          res.json({
            success: true,
            codename: user.codename,
            ...(passkeyData && {
              passkey: {
                backedUp: passkeyData.backedUp,
                backupEligible: deriveBackupEligibility(passkeyData.deviceType),
              },
            }),
            ...(backfill && { backfill }),
          });
    ```

    The existing outer try/catch at lines ~458-462 stays UNCHANGED. The library outer catch handles errors that should still 500 (e.g., `db.getUserById` throw, `passkeyManager.finishAuthentication` throw); BACKFILL-03 containment is implemented as a NESTED try/catch around the hook call only, so a hook throw NEVER reaches the outer catch — login succeeds 200 OK with `backfill: skipped`.

    **Anti-pattern guards (do NOT do these):**
    - Do NOT wrap the entire /login/finish handler body in a new try/catch — BACKFILL-03 containment is a NARROW try/catch around the hook call only. Wrapping wider would also catch errors that should still 500 (e.g., createSession throw).
    - Do NOT call `sessionManager.createSession` BEFORE resolving the hook — the hook result must be echoed on the response, which requires the result in scope when `res.json` is called.
    - Do NOT inline a redaction implementation. Reuse the existing `redactErrorMessage` from `./analytics.js` (single source of truth — T-15-03).
    - Do NOT log `ctx` fields. Do NOT log `body.sealingKeyHex`. Do NOT include the hook input or sensitive fields in the WARN log object beyond what `redactErrorMessage(err)` returns. (T-15-01)
    - Do NOT add a `backfill: undefined` literal to the response — use the spread-guard idiom `...(backfill && { backfill })` (mirrors the Phase 11 `passkey?` spread idiom at line ~451).
    - Do NOT change the hook fire ordering relative to Phase 14: `afterAuthSuccess` MUST run FIRST (gates the rest of the flow), then `backfillKeyBundle` runs only on the `continue:true` branch. Inverting the order would let backfill run before a `continue:false` short-circuit, leaking work.
    - Do NOT widen the hook beyond `await config.hooks.backfillKeyBundle(...)` — no `Promise.race` timeout, no fire-and-forget. The result must be echoed; the await is part of the response shape contract. (Timeout deferred to v0.8 per STATE.md "Login DoS if hook hangs" known limitation.)
    - Do NOT add a register-side fire site. BACKFILL-01 explicitly fires only inside /login/finish.
  </action>
  <verify>
    <automated>cd /home/vitalpointai/projects/near-phantom-auth &amp;&amp; bash -lc 'source ~/.nvm/nvm.sh &amp;&amp; nvm use 20 &amp;&amp; npm run typecheck &amp;&amp; npm test -- --run src/__tests__/registration-auth.test.ts src/__tests__/analytics-lifecycle.test.ts src/__tests__/second-factor-login.test.ts'</automated>
  </verify>
  <acceptance_criteria>
    - Import augmented: `grep -c "import { wrapAnalytics, redactErrorMessage } from './analytics.js';" src/server/router.ts` == 1
    - Old import line removed: `grep -c "^import { wrapAnalytics } from './analytics.js';$" src/server/router.ts` == 0
    - sealingKeyHex destructure present in /login/finish: `awk '/router.post..\\/login\\/finish/,/^  }\\);$/' src/server/router.ts | grep -c "const { challengeId, response, sealingKeyHex } = body;"` == 1
    - `grep -c "config.hooks\\?.backfillKeyBundle" src/server/router.ts` >= 1
    - `grep -c "await config.hooks.backfillKeyBundle" src/server/router.ts` >= 1 (Pitfall 8 await present — same hygiene as Phase 14)
    - Hook ctx passes all 5 required fields by name: `awk '/await config.hooks.backfillKeyBundle/,/\\}\\)/' src/server/router.ts | grep -c "userId:" ` >= 1 AND `awk '/await config.hooks.backfillKeyBundle/,/\\}\\)/' src/server/router.ts | grep -c "codename:"` >= 1 AND `awk '/await config.hooks.backfillKeyBundle/,/\\}\\)/' src/server/router.ts | grep -c "nearAccountId:"` >= 1 AND `awk '/await config.hooks.backfillKeyBundle/,/\\}\\)/' src/server/router.ts | grep -c "sealingKeyHex,"` >= 1 AND `awk '/await config.hooks.backfillKeyBundle/,/\\}\\)/' src/server/router.ts | grep -c "req,"` >= 1
    - Containment via try/catch around hook call: `awk '/Phase 15 BACKFILL-01..03 fire point/,/^          \\}$/' src/server/router.ts | grep -c "try {"` >= 1 AND `awk '/Phase 15 BACKFILL-01..03 fire point/,/^          \\}$/' src/server/router.ts | grep -c "} catch (err) {"` >= 1
    - WARN log uses redactErrorMessage: `grep -c "log.warn({ err: redactErrorMessage(err) }, 'backfill hook threw');" src/server/router.ts` == 1
    - Skipped fallback set on catch: `grep -c "backfill = { backfilled: false, reason: 'skipped' };" src/server/router.ts` >= 1
    - Response spread-guard appended: `awk '/router.post..\\/login\\/finish/,/^  }\\);$/' src/server/router.ts | grep -c "...(backfill && { backfill })"` == 1
    - The fire MUST appear AFTER the secondFactor short-circuit AND BEFORE sessionManager.createSession in the login handler — verified by line ordering: `awk '/router.post..\\/login\\/finish/{f=1} f && /if \\(secondFactor\\) {/{a=NR} f && /Phase 15 BACKFILL-01..03 fire point/{b=NR} f && /sessionManager.createSession\\(user.id/{c=NR; exit} END{exit (a<b && b<c)?0:1}' src/server/router.ts` returns 0
    - The fire MUST be gated on sealingKeyHex AND optional-chain on the hook (both gates required): `grep -c "if (sealingKeyHex && config.hooks?.backfillKeyBundle)" src/server/router.ts` == 1
    - No log of ctx fields anywhere in the file (filter comments first): `grep -v "^[[:space:]]*//" src/server/router.ts | grep -v "^[[:space:]]*\\*" | grep -nE "log\\.(info|warn|error)\\([^)]*ctx" | wc -l` == 0
      - Note: Static grep is best-effort (does not span multi-line log payloads or trailing-`//` comments or block-style `/* */` comments); runtime ctx-substring assertions in `src/__tests__/backfill-redaction.test.ts` are the authoritative defense and catch what this gate cannot.
    - No log of sealingKeyHex anywhere (filter comments first): `grep -v "^[[:space:]]*//" src/server/router.ts | grep -v "^[[:space:]]*\\*" | grep -nE "log\\.(info|warn|error)\\([^)]*sealingKeyHex" | wc -l` == 0
    - No accidental register-side fire (Phase 15 only fires on /login/finish): `awk '/router.post..\\/register\\/finish/,/^  }\\);$/' src/server/router.ts | grep -c "backfillKeyBundle"` == 0
    - Phase 14 secondFactor short-circuit branch is UNCHANGED — `awk '/router.post..\\/login\\/finish/,/^  }\\);$/' src/server/router.ts | grep -c "if (secondFactor)"` == 1 AND `awk '/router.post..\\/login\\/finish/,/^  }\\);$/' src/server/router.ts | grep -c "...secondFactor.body,"` == 1
    - `nvm use 20 && npm run typecheck` exits 0
    - `nvm use 20 && npm test -- --run src/__tests__/registration-auth.test.ts src/__tests__/analytics-lifecycle.test.ts src/__tests__/second-factor-login.test.ts` exits 0 — Phase 11/13/14 login surface unchanged. The Phase 14 second-factor-login tests do not configure backfillKeyBundle, so the optional-chain guard must hold (silent absence path).
  </acceptance_criteria>
  <done>
    /login/finish fires `backfillKeyBundle` after the Phase 14 secondFactor short-circuit and before createSession, gated on body.sealingKeyHex and on the hook being configured. Throws are contained: log warn with redactErrorMessage, fall through to `backfill: { backfilled: false, reason: 'skipped' }`, login response is 200 OK and createSession runs. Response shape is additive — existing `success`, `codename`, `passkey?`, and Phase 14 `secondFactor?` are unchanged. Phase 11/13/14 tests still pass.
  </done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client -> API | POST body validated by zod (existing); request body is untrusted, including the optional sealingKeyHex |
| library -> consumer hook | Library passes userId, codename, nearAccountId, sealingKeyHex, req to consumer's hook code (intended exposure; not a leak) |
| library logging surface | Library MUST NEVER log ctx fields — particularly sealingKeyHex (sensitive material) |
| hook throw -> library response | Library catches throw, logs redacted, continues login (BACKFILL-03 containment boundary) |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|-----------|----------|-----------|----------|-------------|-----------------|
| T-15-01 | Information Disclosure (PII / sensitive material in library log payload) | log.warn at the catch site + fire site | high | mitigate | Plan 15-02 uses `redactErrorMessage(err)` for the WARN payload (Error.name + first 2 stack frames; never err.message). NO `log.*({ ctx })` call appears at the fire site — verified by acceptance criteria filtering comments and asserting 0 occurrences of `log.<level>(...ctx)` and `log.<level>(...sealingKeyHex)`. Plan 15-03 redaction test re-verifies this at runtime by capturing pino output and asserting zero hex substrings. |
| T-15-02 | Denial of Service (login DoS via hook hang — no timeout) | `await config.hooks.backfillKeyBundle(...)` | medium | accept (documented) | The hook is awaited with no Promise.race timeout. A hung consumer hook holds the response until the consumer's runtime kills the request. Per STATE.md "Login DoS if hook hangs (no timeout) — flag as a known limitation"; deferred to v0.8 (consumer mitigation: hooks must be idempotent + finite-time, documented in Plan 15-04 README). Plan 15-02 does NOT add a timeout — adding one would change the contract and require additional testing surface this milestone explicitly defers. |
| T-15-03 | Information Disclosure (sealingKeyHex leak via Error.message in stacked errors) | redactErrorMessage helper + WARN log | high | mitigate | The redactErrorMessage helper from src/server/analytics.ts:109-119 filters V8 stack frames so err.message NEVER reaches the log. A consumer that throws `new Error(`failed for ${ctx.sealingKeyHex}`)` would still have its message stripped from the captured log payload. Plan 15-02 acceptance criterion: WARN log uses `redactErrorMessage(err)` literally (single source of truth). Plan 15-03 redaction test asserts zero hex substring in captured log output as a runtime check. |
| T-15-06 | Tampering (BACKFILL-03 contract regression — login blocks on hook throw) | nested try/catch around hook call | high | mitigate | Plan 15-02 implements the BACKFILL-03 containment as a NARROW try/catch around the hook call only. The catch block sets `backfill = { backfilled: false, reason: 'skipped' }` and falls through to createSession + res.json (login completes 200 OK). Acceptance criterion: explicit grep gates for the catch block, the WARN log line, the skipped fallback assignment. Plan 15-03 BACKFILL-03 tests assert end-to-end that throw -> 200 OK + skipped echo + createSession invocation (real Set-Cookie emission is the SessionManager implementation's responsibility, not asserted at this layer). |
| T-15-07 | Tampering (silent skip violation — hook fires when sealingKeyHex absent) | `if (sealingKeyHex && config.hooks?.backfillKeyBundle)` gate | medium | mitigate | Plan 15-02 gates the hook on BOTH sealingKeyHex truthy AND the hook being configured. Acceptance criterion: literal `if (sealingKeyHex && config.hooks?.backfillKeyBundle)` appears exactly once. Plan 15-03 silent-skip tests assert hook NOT called when either gate fails. |
| T-15-09 | Tampering (sequential-fire ordering broken — backfill fires on continue:false) | location of BACKFILL fire relative to Phase 14 short-circuit | medium | mitigate | Plan 15-02 inserts the BACKFILL fire AFTER the `if (secondFactor) { return ... }` short-circuit (which RETURNs on continue:false). Acceptance criterion: line ordering `if (secondFactor) {` < `Phase 15 BACKFILL-01..03 fire point` < `sessionManager.createSession(user.id`. Inverting this order would let backfill run on the short-circuit path. |

**HIGH severity threats: T-15-01, T-15-03, T-15-06.** All three are mitigated in Plan 15-02 with explicit grep gates plus runtime tests in Plan 15-03 (redaction defense + containment defense). T-15-02 is accepted as documented; surfaced as a known limitation in Plan 15-04 README. The remaining threats land in their natural plans (T-15-04 in 15-01 type contract; T-15-05 IPFS dual-recovery in 15-04 README; T-15-08 await race already covered by `await` literal in the action).
</threat_model>

<verification>
- `nvm use 20 && npm run typecheck` exits 0
- `nvm use 20 && npm test -- --run src/__tests__/registration-auth.test.ts src/__tests__/analytics-lifecycle.test.ts src/__tests__/second-factor-login.test.ts` exits 0 — full suite of Phase 11/13/14 register/login tests pass without regression
- All grep + awk acceptance gates from Task 1 pass
- The Wave 0 stub tests (`backfill-login.test.ts`, `backfill-redaction.test.ts`) still skip cleanly (still all `it.todo`); Plan 15-03 will turn them green
- Phase 14 tests (second-factor-login.test.ts, second-factor-register.test.ts) still pass — they do not configure backfillKeyBundle, so the optional-chain guard exercised by silent-absence path
</verification>

<success_criteria>
- BACKFILL-01 wired: hook fires inside /login/finish gated on body.sealingKeyHex AND configured hook; otherwise silent skip
- BACKFILL-02 wired: hook result echoed on response under additive `backfill: { backfilled, reason }` key
- BACKFILL-03 wired: hook throw caught, redactErrorMessage WARN log, backfill set to `{ backfilled: false, reason: 'skipped' }`, login proceeds normally — `SessionManager.createSession` IS still called (its Set-Cookie emission is the SessionManager implementation's responsibility) and the response status is 200 OK
- T-15-01 + T-15-03 enforced at the source level (grep gates) and ready for runtime verification in Plan 15-03
- Sequential-fire ordering preserved: Phase 14 afterAuthSuccess runs first; on continue:true, Phase 15 backfillKeyBundle runs next; on continue:false, the short-circuit RETURNS before backfill is invoked
- Phase 11/12/13/14 tests all green; typecheck green; no log of ctx fields or sealingKeyHex
</success_criteria>

<output>
After completion, create `.planning/phases/15-lazy-backfill-hook/15-02-SUMMARY.md` documenting:
- The exact diff blocks inserted (Step A import, Step B destructure, Step C fire site, Step D response spread)
- The sequential-fire ordering choice (afterAuthSuccess gates backfill — short-circuit RETURNs before backfill is invoked)
- The decision to reuse `redactErrorMessage` from src/server/analytics.ts (single source of truth) rather than inline a separate helper
- The deferral of hook timeout to v0.8 (T-15-02 accepted as documented)
- A line-count diff for src/server/router.ts (file is expected to grow ~30-40 lines)
- Confirmation that the existing analytics-lifecycle.test.ts, registration-auth.test.ts, and second-factor-login.test.ts pass with no modification
</output>
</content>
</invoke>