---
phase: 02-input-validation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/server/validation/schemas.ts
  - src/server/validation/validateBody.ts
  - src/__tests__/validation.test.ts
autonomous: true
requirements: [SEC-05]

must_haves:
  truths:
    - "Zod 4 is installed as a production dependency"
    - "13 named schemas exist covering all POST routes that accept a body"
    - "WebAuthn credential response schemas use .passthrough(), not .strict()"
    - "validateBody helper returns typed data on success or sends 400 on failure"
    - "Schema tests prove valid payloads parse and invalid payloads produce errors"
  artifacts:
    - path: "src/server/validation/schemas.ts"
      provides: "13 named Zod schemas for all POST route bodies"
      exports: ["registerStartBodySchema", "registerFinishBodySchema", "loginStartBodySchema", "loginFinishBodySchema", "logoutBodySchema", "walletLinkBodySchema", "walletVerifyBodySchema", "walletStartBodySchema", "walletFinishBodySchema", "ipfsSetupBodySchema", "ipfsRecoverBodySchema", "oauthCallbackBodySchema", "oauthLinkBodySchema"]
    - path: "src/server/validation/validateBody.ts"
      provides: "Generic validateBody<T>(schema, req, res) helper"
      exports: ["validateBody"]
    - path: "src/__tests__/validation.test.ts"
      provides: "Unit tests for schema acceptance and rejection"
  key_links:
    - from: "src/server/validation/schemas.ts"
      to: "zod"
      via: "import { z } from 'zod'"
      pattern: "import.*from.*zod"
    - from: "src/server/validation/validateBody.ts"
      to: "zod"
      via: "safeParse call"
      pattern: "schema\\.safeParse"
---

<objective>
Install Zod 4, create all 13 request body schemas for POST routes, create the validateBody helper, and write unit tests proving schemas accept valid payloads and reject invalid ones.

Purpose: Establishes the validation infrastructure that Plan 02 wires into route handlers, fulfilling SEC-05.
Output: schemas.ts (13 exports), validateBody.ts (helper), validation.test.ts (schema tests), zod in package.json
</objective>

<execution_context>
@/home/vitalpointai/.claude/get-shit-done/workflows/execute-plan.md
@/home/vitalpointai/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-input-validation/02-RESEARCH.md

@src/server/router.ts
@src/server/oauth/router.ts
@package.json
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Install Zod and create schemas + validateBody helper</name>
  <files>package.json, package-lock.json, src/server/validation/schemas.ts, src/server/validation/validateBody.ts</files>
  <behavior>
    - registerFinishBodySchema accepts {challengeId: "abc", tempUserId: "xyz", codename: "ALPHA", response: {id: "x", rawId: "y", type: "public-key", response: {clientDataJSON: "...", attestationObject: "..."}, clientExtensionResults: {}}} and parses successfully
    - registerFinishBodySchema accepts response with extra unknown browser extension properties (passthrough, not strict)
    - registerFinishBodySchema rejects missing challengeId (safeParse returns success: false)
    - loginFinishBodySchema rejects challengeId: 123 (number instead of string)
    - loginStartBodySchema accepts empty object {} (codename is optional)
    - loginStartBodySchema accepts {codename: "ALPHA"}
    - logoutBodySchema, walletLinkBodySchema, walletStartBodySchema accept empty object {}
    - walletVerifyBodySchema rejects missing signature field
    - walletFinishBodySchema requires signature, challenge, nearAccountId as strings
    - ipfsSetupBodySchema requires password as string
    - ipfsRecoverBodySchema requires cid and password as strings
    - oauthCallbackBodySchema requires code and state as strings
    - oauthLinkBodySchema requires code as string; state and codeVerifier are optional
    - validateBody returns typed data when schema passes
    - validateBody sends res.status(400).json({error: "..."}) and returns null on failure
  </behavior>
  <action>
    1. Run `npm install zod@^4.3.6` to add zod as a production dependency.

    2. Create `src/server/validation/schemas.ts` with 13 named schema exports. Key rules:
       - Use `z.string().min(1)` for required string fields (catches empty strings that truthiness guards miss).
       - WebAuthn `response` objects in registerFinishBodySchema and loginFinishBodySchema MUST use `.passthrough()` on both the outer credential object and the inner `response` sub-object. Per user decision: no `.strict()` on WebAuthn response fields.
       - `clientExtensionResults` typed as `z.record(z.unknown())` to allow any extension data.
       - Routes with no body (register/start, logout, wallet/link, wallet/start) use `z.object({})`.
       - loginStartBodySchema uses `z.object({ codename: z.string().min(1).optional() })`.
       - oauthLinkBodySchema: `code` required, `state` and `codeVerifier` optional strings.

    3. Create `src/server/validation/validateBody.ts`:
       ```typescript
       import type { Request, Response } from 'express';
       import type { ZodTypeAny, z } from 'zod';

       export function validateBody<T extends ZodTypeAny>(
         schema: T,
         req: Request,
         res: Response,
       ): z.infer<T> | null {
         const result = schema.safeParse(req.body);
         if (!result.success) {
           res.status(400).json({
             error: result.error.issues[0]?.message ?? 'Invalid request body',
           });
           return null;
         }
         return result.data;
       }
       ```

    4. Write tests in `src/__tests__/validation.test.ts` covering every behavior listed above. Tests import schemas directly and call `.safeParse()` — no HTTP mocking needed for schema tests. For validateBody tests, mock `req` and `res` objects.
  </action>
  <verify>
    <automated>npx vitest run src/__tests__/validation.test.ts</automated>
  </verify>
  <done>
    - zod@^4.3.6 in package.json dependencies
    - schemas.ts exports 13 named schemas
    - All WebAuthn response schemas use .passthrough(), none use .strict()
    - validateBody.ts exports the helper function
    - All validation tests pass (valid payloads accepted, invalid rejected, passthrough works)
  </done>
</task>

</tasks>

<verification>
- `npx vitest run src/__tests__/validation.test.ts` passes with all tests green
- `grep -c "export const.*Schema" src/server/validation/schemas.ts` returns 13
- `grep "strict()" src/server/validation/schemas.ts` returns no results
- `grep "passthrough()" src/server/validation/schemas.ts` returns results for WebAuthn schemas
- `grep '"zod"' package.json` shows zod in dependencies (not devDependencies)
</verification>

<success_criteria>
All 13 schemas parse valid payloads and reject invalid ones. WebAuthn schemas allow unknown browser extension properties. validateBody helper correctly returns typed data or sends 400. Zod installed as production dependency.
</success_criteria>

<output>
After completion, create `.planning/phases/02-input-validation/02-01-SUMMARY.md`
</output>
