---
phase: 10
plan: 06
type: execute
wave: 5
depends_on:
  - 10-01
  - 10-02
  - 10-03
  - 10-04
  - 10-05
files_modified:
  - package.json
  - CHANGELOG.md
  - README.md
autonomous: false
requirements:
  - MPC-12
tags:
  - publish
  - release
  - documentation
user_setup:
  - service: npm
    why: "Publishing the v0.6.1 hotfix to the public registry; only the user has the npm token and 2FA device"
    env_vars:
      - name: NPM_TOKEN
        source: "User's npm account → Access Tokens → Automation token (preferred for CI; this plan uses interactive login as fallback)"
    dashboard_config:
      - task: "Confirm 2FA device is reachable for npm publish prompt"
        location: "User's npm account 2FA settings"
must_haves:
  truths:
    - "package.json version is 0.6.1 exactly (not 0.6.1-rc, not 0.7.0)"
    - "CHANGELOG.md exists with a v0.6.1 entry that calls out the additive MPCAccountManager surface"
    - "README.md contains an MPCAccountManager section documenting derivation, config, and security expectations"
    - "npm run build succeeds; dist/ contains the value export of MPCAccountManager"
    - "After Plan 05's leak audit Gate 1 runs against the freshly-built dist, it passes (zero treasuryPrivateKey leaks)"
    - "npm publish succeeds at v0.6.1 (this is the autonomous: false checkpoint — user runs the publish + 2FA)"
    - "A fresh-consumer smoke install in a temporary directory can import { MPCAccountManager } from '@vitalpoint/near-phantom-auth/server' and the import resolves to a function (typeof === 'function')"
  artifacts:
    - path: "package.json"
      provides: "Version bump to 0.6.1"
      contains: "\"version\": \"0.6.1\""
    - path: "CHANGELOG.md"
      provides: "v0.6.1 release notes (Keep A Changelog format; first entry in repo)"
      min_lines: 30
      contains: "## [0.6.1]"
    - path: "README.md"
      provides: "MPCAccountManager consumer documentation section"
      contains: "MPCAccountManager"
  key_links:
    - from: "package.json"
      to: "npm registry"
      via: "npm publish (interactive — user owns the token + 2FA)"
      pattern: "\"version\": \"0\\.6\\.1\""
    - from: "README.md"
      to: "MPC-12 documentation requirement"
      via: "MPCAccountManager section"
      pattern: "## MPCAccountManager|### MPCAccountManager"
---

<objective>
Bump the package to v0.6.1, write the CHANGELOG.md (creating it from scratch since the repo has none), append the MPCAccountManager documentation section to README.md, run a clean build, gate the build with Plan 05's leak audit Gate 1, and (in the human-checkpoint task) publish to npm and verify the smoke install.

Purpose: Closes MPC-12 — the release task. Everything before this is a no-op for the downstream consumer until v0.6.1 is on the npm registry. The publish step requires a human (npm token + 2FA), so this plan is `autonomous: false` and uses a `checkpoint:human-action` task for the publish + smoke verification.

Output:
  - `package.json` version bumped to 0.6.1 (was 0.6.0)
  - `CHANGELOG.md` created with Keep A Changelog format; first entry is v0.6.1
  - `README.md` gains an `## MPCAccountManager` section documenting the class, derivation, config, security expectations, and a code example
  - `dist/` is freshly built and contains the value export of MPCAccountManager
  - npm publish succeeds at v0.6.1 (human-driven)
  - Fresh-consumer smoke install in a tempdir confirms `typeof MPCAccountManager === 'function'`
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md
@.planning/phases/10-mpcaccountmanager/10-RESEARCH.md
@.planning/phases/10-mpcaccountmanager/10-01-SUMMARY.md
@.planning/phases/10-mpcaccountmanager/10-04-SUMMARY.md
@.planning/phases/10-mpcaccountmanager/10-05-SUMMARY.md
@package.json
@README.md

<interfaces>
<!-- Current package.json (line 3): "version": "0.6.0" -->
<!-- Required after this plan: "version": "0.6.1" -->

<!-- CHANGELOG.md does not exist; create from scratch in Keep A Changelog format: -->
```markdown
# Changelog

All notable changes to this project will be documented in this file.

The format is based on [Keep a Changelog](https://keepachangelog.com/en/1.1.0/),
and this project adheres to [Semantic Versioning](https://semver.org/spec/v2.0.0.html).

## [0.6.1] — 2026-04-28

### Fixed
- **MPC-01:** `MPCAccountManager` is now exported as a runtime value (was previously
  `export type` which stripped the constructor at compile time, producing
  `TypeError: MPCAccountManager is not a constructor` for consumers using
  `import { MPCAccountManager }`). Closes the production restart loop reported
  by the Ledgera mpc-sidecar consumer.
- **MPC-05:** `verifyRecoveryWallet` now correctly returns `false` for
  FunctionCall-only access keys. Previously it returned `true` for any non-error
  RPC response, allowing FunctionCall keys to satisfy recovery verification.

### Added
- **MPC-07:** New `MPCAccountManagerConfig` type alias makes `derivationSalt` REQUIRED
  at the consumer-facing type boundary (cross-tenant isolation guarantee).
- **MPC-09:** Pino redact paths added to the default logger
  (`config.treasuryPrivateKey`, `*.treasuryPrivateKey`); raw treasury key string
  is no longer retained as an instance field — replaced by a KeyPair object.
- **MPC-08:** yoctoNEAR conversion now uses `parseNearAmount` from `@near-js/utils`.
- **MPC-06:** `createAccount` retries `view_account` once after a nonce-race
  broadcast failure, allowing concurrent calls from multiple replicas to converge
  to a single provisioned account.
- **MPC-10:** `createAccount` now throws classified errors with `cause` set
  (`RPC unreachable`, `Treasury underfunded`, `Transfer failed`) instead of
  returning a degraded `{ mpcPublicKey: 'creation-failed' }` object.
- New consumer-facing type alias `CreateAccountResult` (= `MPCAccount`) frozen
  for the contract.
- Test coverage: T1–T12 scenarios in `src/__tests__/mpc-account-manager.test.ts`;
  treasury-leak audit in `src/__tests__/mpc-treasury-leak.test.ts`;
  exports regression in `src/__tests__/exports.test.ts`.

### Notes
- Additive only — all v0.6.0 exports remain unchanged. The contract
  (`createAccount`, `verifyRecoveryWallet`, `MPCAccountManagerConfig`,
  `CreateAccountResult`) is FROZEN by consumer pin.
- Zero new npm dependencies. `parseNearAmount` and KeyPair utilities were
  already available via `@near-js/utils` and `@near-js/crypto`.
- Internal usage of `createMPCManager(MPCConfig)` in `createAnonAuth` is
  unchanged; the new `MPCAccountManagerConfig` is a strict-subset alias.

## [0.6.0] — 2026-03-15
- WebAuthn PRF extension for DEK sealing key (Phase 9; PRF-01 through PRF-12).

## [0.5.x] — 2026-03-14 and earlier
- Initial hardening milestone (35 requirements). See `.planning/v0.5-MILESTONE-AUDIT.md`.
```

<!-- README.md MPCAccountManager section to append -->
```markdown
## MPCAccountManager (v0.6.1+)

Standalone helper for provisioning NEAR implicit accounts and verifying recovery
wallets. Exported from `@vitalpoint/near-phantom-auth/server` as a runtime value.

### Quick start

```typescript
import { MPCAccountManager, type MPCAccountManagerConfig, type CreateAccountResult } from '@vitalpoint/near-phantom-auth/server';

const manager = new MPCAccountManager({
  networkId: 'testnet',                      // or 'mainnet'
  treasuryAccount: process.env.NEAR_TREASURY_ACCOUNT!,
  treasuryPrivateKey: process.env.NEAR_TREASURY_KEY!,
  derivationSalt: process.env.NEAR_DERIVATION_SALT!,  // REQUIRED — see Security
  fundingAmount: '0.01',                     // optional; default '0.01' NEAR
});

const result: CreateAccountResult = await manager.createAccount('user-id');
// result.nearAccountId matches /^[a-f0-9]{64}$/  (64-char hex implicit account)
// result.mpcPublicKey is 'ed25519:BASE58_ENCODED_PUBKEY'
// result.derivationPath is 'near-anon-auth,user-id'
// result.onChain is true after successful funding
```

### Derivation function

```
seedInput      = `implicit-${derivationSalt}-${userId}`
seed           = SHA-256(seedInput)
publicKeyBytes = first 32 bytes of SHA-512(seed)
nearAccountId  = publicKeyBytes.toString('hex')         // 64-char lowercase hex
mpcPublicKey   = `ed25519:${bs58.encode(publicKeyBytes)}`
derivationPath = `near-anon-auth,${userId}`
```

### Idempotency (MPC-03)

`createAccount(userId)` is idempotent — a second call against an already-provisioned
account short-circuits via `view_account` and issues zero additional transfers.

### Concurrent calls (MPC-06)

Two concurrent `createAccount` calls for the same `userId` from different replicas
converge to a single provisioned account. The loser of the nonce race retries
`view_account` once and returns success.

### Error paths (MPC-10)

`createAccount` throws when:
- The NEAR RPC is unreachable: `Error('RPC unreachable', { cause })`
- The treasury is underfunded: `Error('Treasury underfunded', { cause })`
- Any other broadcast failure: `Error('Transfer failed', { cause })`

`verifyRecoveryWallet` throws ONLY when the NEAR RPC is unreachable (consumer
should return 500). It returns `false` (does not throw) for missing accounts,
function-call-only keys, or unknown access keys.

### Security expectations

- **`derivationSalt` is REQUIRED** at the type level (TypeScript rejects a
  config without it). Use a per-tenant secret salt to prevent cross-tenant
  account ID collision.
- **`treasuryPrivateKey` is never logged** — the manager replaces the raw
  string with a KeyPair object on construction; Pino redact paths are wired
  into the default logger.
- **Transactions are signed in-process** — no `near-cli` shell-out, no
  `process.exec` injection vector.
- **`verifyRecoveryWallet` returns true ONLY for FullAccess keys** — function-call
  scoped keys cannot satisfy recovery verification.

### Frozen contract (consumer pin)

The following surface is FROZEN — no field, method, or return-shape rename
without a coordinated PR:
- `class MPCAccountManager`
- `createAccount(userId: string): Promise<CreateAccountResult>`
- `verifyRecoveryWallet(nearAccountId: string, publicKey: string): Promise<boolean>`
- `interface MPCAccountManagerConfig` (with `derivationSalt: string` REQUIRED)
- `type CreateAccountResult` (= `MPCAccount`)
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Bump version, create CHANGELOG.md, append MPCAccountManager section to README.md, run a clean build</name>
  <files>package.json, CHANGELOG.md, README.md</files>
  <read_first>
    - package.json (lines 1-15 — verify version is currently "0.6.0")
    - README.md (full — find a good insertion point for the new section, typically before any "License" section near the end)
    - .planning/phases/10-mpcaccountmanager/10-04-SUMMARY.md and 10-05-SUMMARY.md (confirm prior plans landed)
    - .planning/REQUIREMENTS.md (lines 11-37 — MPC-01 through MPC-12 phrasing for accurate CHANGELOG)
  </read_first>
  <behavior>
    - package.json `"version"` field changes from "0.6.0" to "0.6.1"
    - CHANGELOG.md is created (it does not exist before this task)
    - README.md gains a new top-level `## MPCAccountManager (v0.6.1+)` section near the end of the file, before any license/footer
    - `npm run build` succeeds
    - The freshly-built `dist/server/index.js` exports MPCAccountManager as a function value
    - Plan 05's leak audit Gate 1 passes against the new dist
  </behavior>
  <action>
    Step A — Bump version in package.json. Use the Edit tool on `package.json` to change the `"version"` line from `"version": "0.6.0",` to `"version": "0.6.1",`. Do not change any other field.

    Alternative: run `nvm use 20 && npm version 0.6.1 --no-git-tag-version` which updates package.json AND package-lock.json atomically without creating a git tag. Prefer this command — it handles package-lock.json synchronization that the manual Edit would miss.

    Step B — Create `CHANGELOG.md` at the project root with the EXACT content from the `<interfaces>` block above (the Keep A Changelog formatted entry). Use the Write tool. The file does not exist; this is a fresh creation.

    Step C — Append the MPCAccountManager documentation section to `README.md`. Read the file first to identify the insertion point. Append the section content from the `<interfaces>` block at the END of the file (or just before any final "License" / "Contributing" section if those exist; keep alphabetical/topical order if README has a TOC).

    Step D — Run a clean build:
    ```
    nvm use 20 && rm -rf dist && npm run build
    ```
    Confirm exit code 0.

    Step E — Verify the dist build is correct (regression-prevention):
    ```
    nvm use 20 && node -e "import('./dist/server/index.js').then(m => { if (typeof m.MPCAccountManager !== 'function') { console.error('FAIL: MPCAccountManager is', typeof m.MPCAccountManager); process.exit(1); } else { console.log('OK: MPCAccountManager is a function'); } }).catch(e => { console.error('FAIL: import failed', e.message); process.exit(1); })"
    ```
    Expected output: `OK: MPCAccountManager is a function`. Exit code 0.

    Step F — Run the leak audit gate against the new dist:
    ```
    nvm use 20 && grep -rn "treasuryPrivateKey" dist/server/ --include="*.js" --include="*.cjs" || echo "GATE 1 PASS: zero matches"
    ```
    Expected output: `GATE 1 PASS: zero matches`. If grep returns matches, INVESTIGATE — there should be no static reference to the field in the runtime bundle (the .d.ts may mention it as a type field name, which is acceptable).

    Step G — Run the full test suite once more to confirm nothing regressed:
    ```
    nvm use 20 && npm test -- --run
    ```
    Expected: 0 failures. The treasury-leak Gate 1 should now ACTIVATE (dist/ exists) and pass.
  </action>
  <verify>
    <automated>nvm use 20 && grep -E "\"version\": \"0\\.6\\.1\"" package.json | wc -l | grep -q "^1$" && test -f CHANGELOG.md && grep -c "MPCAccountManager" README.md | grep -qE "^[1-9][0-9]*$" && test -f dist/server/index.js && node -e "import('./dist/server/index.js').then(m => process.exit(typeof m.MPCAccountManager === 'function' ? 0 : 1))" && grep -rn "treasuryPrivateKey" dist/server/ --include="*.js" --include="*.cjs" | head -1 | grep -qv "."; echo "exit=$?"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -E "\"version\": \"0\\.6\\.1\"" package.json | wc -l` returns 1 (version bumped)
    - `grep -E "\"version\": \"0\\.6\\.0\"" package.json | wc -l` returns 0 (old version gone)
    - `test -f CHANGELOG.md` succeeds
    - `grep -c "## \[0\.6\.1\]" CHANGELOG.md` returns 1 (the new entry exists)
    - `grep -c "MPC-01:\|MPC-05:\|MPC-09:\|MPC-10:" CHANGELOG.md` returns >= 4 (key requirement IDs called out)
    - `grep -c "MPCAccountManager" README.md` returns >= 5 (section + code examples)
    - `grep -c "## MPCAccountManager\|### MPCAccountManager" README.md` returns >= 1 (section header present)
    - `test -f dist/server/index.js` succeeds
    - `node -e "import('./dist/server/index.js').then(m => process.exit(typeof m.MPCAccountManager === 'function' ? 0 : 1))"` exits 0
    - `grep -rn "treasuryPrivateKey" dist/server/ --include="*.js" --include="*.cjs"` returns no output (or empty stdout); the exit code may be 1 (no match)
    - `grep -rn "ed25519:[A-Za-z0-9]\{40,\}" dist/ --include="*.js" --include="*.cjs"` returns no output
    - `nvm use 20 && npm test -- --run` exits 0; total test count >= 252 + new tests; treasury-leak Gate 1 now activates and passes
  </acceptance_criteria>
  <done>
    package.json bumped to 0.6.1, CHANGELOG.md created with v0.6.1 entry, README.md has new MPCAccountManager section, npm run build succeeds, dist/server/index.js exports MPCAccountManager as a function, leak audit Gate 1 passes against the freshly-built dist, full test suite stays green.
  </done>
</task>

<task type="checkpoint:human-action" gate="blocking">
  <name>Task 2: Publish v0.6.1 to npm and verify a fresh-consumer smoke install</name>
  <files>(none — Task 2 is a human-action checkpoint that runs npm publish; no source/text files are modified)</files>
  <read_first>
    - .planning/phases/10-mpcaccountmanager/10-06-PLAN.md (this file — Task 1's outputs)
    - package.json (verify version 0.6.1)
    - dist/server/index.js (verify MPCAccountManager is a runtime value)
  </read_first>
  <what-built>
    Task 1 has produced:
    - package.json at version 0.6.1
    - CHANGELOG.md with the v0.6.1 entry
    - README.md with the MPCAccountManager section
    - dist/ freshly built with MPCAccountManager as a runtime value export
    - All tests green (252+ existing + Phase 10's new tests)
    - Plan 05's leak audit Gate 1 passing against the new dist
  </what-built>
  <action>
    This is a `checkpoint:human-action` task. The implementer (Claude) MUST NOT run `npm publish` directly — npm publish requires the user's npm token + 2FA prompt, which is outside Claude's reach. Instead, Claude pauses execution and presents the human-driven steps below in `<how-to-verify>`. The user runs the steps in order, then types the resume signal.

    Why this is checkpoint:human-action and not auto:
    - `npm publish` requires a token tied to the user's npm account
    - 2FA prompt is interactive and cannot be answered by an automation
    - The smoke-install verification involves creating a tempdir on the user's machine and reading interactive output

    Claude's responsibility before pausing:
    - Confirm Task 1's acceptance criteria all pass (re-run the verify command if uncertain)
    - Confirm `npm whoami` returns a username (if it returns nothing, the user must `npm login` first — surface this as guidance)
    - Run `nvm use 20 && npm publish --dry-run --access public 2>&1 | head -40` and include the output in the checkpoint message (informational only)
    - Then pause and wait for the resume signal

    The detailed steps the user runs are listed in `<how-to-verify>` below.
  </action>
  <how-to-verify>
    Step 1 — Confirm you (the user) have npm publish rights for `@vitalpoint/near-phantom-auth`:
    ```
    nvm use 20 && npm whoami
    ```
    The output must be a username with publish rights to the @vitalpoint org.

    Step 2 — Inspect what would be published (dry-run; no network call):
    ```
    nvm use 20 && npm publish --dry-run --access public 2>&1 | head -40
    ```
    Confirm:
    - Package name: `@vitalpoint/near-phantom-auth`
    - Version: `0.6.1`
    - Files include: `dist/`, `README.md`, `package.json`
    - Files do NOT include: `src/`, `node_modules/`, `.planning/`

    Step 3 — Publish (this is the human-only step — npm publish prompts for 2FA if your token requires it):
    ```
    nvm use 20 && npm publish --access public
    ```
    Enter your 2FA OTP when prompted. Expected: `+ @vitalpoint/near-phantom-auth@0.6.1`.

    Step 4 — Verify the registry has the new version:
    ```
    nvm use 20 && npm view @vitalpoint/near-phantom-auth@0.6.1 version
    ```
    Expected output: `0.6.1`.

    Step 5 — Smoke test: a fresh consumer install in a temporary directory.
    ```
    cd $(mktemp -d) && \
      nvm use 20 && \
      npm init -y > /dev/null && \
      npm install @vitalpoint/near-phantom-auth@0.6.1 --silent && \
      node -e "import('@vitalpoint/near-phantom-auth/server').then(m => { console.log('typeof MPCAccountManager:', typeof m.MPCAccountManager); process.exit(typeof m.MPCAccountManager === 'function' ? 0 : 1); }).catch(e => { console.error('FAIL', e.message); process.exit(2); })"
    ```
    Expected output:
    ```
    typeof MPCAccountManager: function
    ```
    Exit code 0.

    Step 6 — Verify the smoke can also INSTANTIATE the class:
    ```
    node -e "import('@vitalpoint/near-phantom-auth/server').then(({ MPCAccountManager }) => { const m = new MPCAccountManager({ networkId: 'testnet', treasuryAccount: 't.testnet', treasuryPrivateKey: 'ed25519:placeholder', derivationSalt: 'smoke' }); console.log('instantiated:', !!m); process.exit(0); }).catch(e => { console.error('FAIL', e.message); process.exit(1); })"
    ```
    Expected: `instantiated: true`. Exit code 0.

    Step 7 — Tag the release in git (after successful publish):
    ```
    git tag v0.6.1 && git push origin v0.6.1
    ```

    Once Steps 1–7 succeed, type the resume signal below.
  </how-to-verify>
  <verify>
    <automated>nvm use 20 && npm view @vitalpoint/near-phantom-auth@0.6.1 version 2>&1 | grep -q "^0\.6\.1$"</automated>
  </verify>
  <acceptance_criteria>
    - `nvm use 20 && npm view @vitalpoint/near-phantom-auth@0.6.1 version` outputs `0.6.1` (the registry has the new version)
    - In a fresh tempdir: `npm install @vitalpoint/near-phantom-auth@0.6.1` succeeds
    - In that tempdir: the smoke import script outputs `typeof MPCAccountManager: function` and exits 0
    - In that tempdir: the instantiation script outputs `instantiated: true` and exits 0
    - `git tag --list v0.6.1` shows the tag locally; `git ls-remote origin v0.6.1` shows the tag on the remote
    - User has confirmed by typing the resume signal
  </acceptance_criteria>
  <done>
    npm publish succeeded at v0.6.1; registry confirms the version; fresh consumer install resolves MPCAccountManager as a function and instantiates without throwing; git tag v0.6.1 pushed to origin. The user has typed the resume signal acknowledging successful completion.
  </done>
  <resume-signal>Type "published" when steps 1–7 complete successfully (or describe issues — e.g., "401 unauthorized on publish" or "smoke install resolved MPCAccountManager as undefined")</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| Local build → npm registry | Publish requires npm token + 2FA; only the user can authorize |
| Published bundle → consumer install | Consumer must receive a runtime value export, not a type-stripped placeholder |
| README/CHANGELOG → consumer expectations | Documentation must match the frozen contract exactly; mismatch = consumer integration break |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-publish-credential-leak | Information Disclosure | npm publish flow | accept | The publish step is `checkpoint:human-action`; the user retains the npm token, never exposed to Claude. 2FA prompt is interactive |
| T-publish-bad-version | Tampering | package.json version field | mitigate | Task 1 acceptance criteria check `"version": "0.6.1"` exactly; npm publish refuses to overwrite an existing version |
| T-publish-stale-dist | Tampering | dist/ contents at publish time | mitigate | Task 1 step D runs `rm -rf dist && npm run build` before any publish; npm publish includes the freshly-built `dist/` per the `files` field in package.json |
| T-leak-in-published-bundle | Information Disclosure | dist/ bundle on npm registry | mitigate | Plan 05 Gate 1 runs against the freshly-built dist BEFORE publish; if a treasuryPrivateKey reference exists, the build verification step fails and publish does not happen |
| T-doc-drift | Tampering (cross-team) | README.md MPCAccountManager section | mitigate | The section explicitly states the FROZEN contract (createAccount signature, MPCAccountManagerConfig fields, error taxonomy). Any future change requires a coordinated CHANGELOG entry |
| T-rollback-needed | Denial of Service | published v0.6.1 with regression | accept | npm allows `npm unpublish @vitalpoint/near-phantom-auth@0.6.1` within 72h; user can roll back manually if a smoke-test failure surfaces post-publish |
</threat_model>

<verification>
- `grep -E "\"version\": \"0\\.6\\.1\"" package.json` returns 1 line
- `test -f CHANGELOG.md` succeeds
- `grep -c "## \\[0\\.6\\.1\\]" CHANGELOG.md` returns 1
- `grep -c "MPCAccountManager" README.md` returns >= 5
- `nvm use 20 && npm run build` exits 0
- `nvm use 20 && npm test -- --run` exits 0
- `node -e "import('./dist/server/index.js').then(m => process.exit(typeof m.MPCAccountManager === 'function' ? 0 : 1))"` exits 0
- `grep -rn "treasuryPrivateKey" dist/server/ --include="*.js" --include="*.cjs"` returns empty
- After Task 2: `npm view @vitalpoint/near-phantom-auth@0.6.1 version` returns `0.6.1`
- After Task 2: smoke install in tempdir reports `typeof MPCAccountManager: function`
</verification>

<success_criteria>
1. package.json at 0.6.1
2. CHANGELOG.md created with v0.6.1 entry citing MPC-01, MPC-05, MPC-06, MPC-08, MPC-09, MPC-10
3. README.md has the MPCAccountManager (v0.6.1+) section with quick-start, derivation function, idempotency, error paths, security expectations, frozen contract
4. dist/ rebuilt clean and validated by Plan 05's Gate 1 (zero treasuryPrivateKey leaks; zero ed25519 key literals)
5. npm publish at 0.6.1 succeeds (human-driven via the human-action checkpoint)
6. Fresh-consumer smoke install in a tempdir confirms `typeof MPCAccountManager === 'function'` AND `new MPCAccountManager({ ... })` does not throw
7. Git tag `v0.6.1` is pushed to origin
</success_criteria>

<output>
After completion, create `.planning/phases/10-mpcaccountmanager/10-06-SUMMARY.md` summarizing:
- Final package.json version (0.6.1)
- CHANGELOG.md and README.md content highlights
- Build size and `dist/server/index.js` size
- Smoke-install result (typeof MPCAccountManager and instantiation OK)
- npm registry timestamp for v0.6.1 publish
- Git tag confirmation
- Note: this is the final plan of Phase 10 / milestone v0.6.1; STATE.md should be updated to reflect milestone closure
</output>
